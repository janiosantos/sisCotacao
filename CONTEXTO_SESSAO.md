# CONTEXTO_SESSAO — Estado do Projeto (leitura obrigatória para agentes)

- **2026-09-03 (publicação v2.38.0)**: staging foi validado com sucesso nas execuções `33708674453` e `33708901167` em modo rápido, passando build, migrations em banco isolado, health, smoke autenticado e reconciliação. A execução completa `33707913969` foi cancelada após mais de 11 minutos sem progresso no gate backend; os testes CI ficaram registrados como pendência de infraestrutura do runner, não como sucesso. A primeira tentativa de produção `33708780901` falhou antes do rollout porque o workflow não passava `APP_VERSION` ao build frontend; a correção foi commitada em `9ea8c7b`. A segunda execução de produção `33708968808` concluiu com sucesso: backup condicional, build/tag, migrations até o schema **154**, rollout, health, smoke e registro da release `v2.38.0`. Produção está rodando a release autorizada; nenhum downgrade ou operação manual foi executado.

- **2026-09-03 (recebimento exclusivamente no Caixa)**: removidos da tela `Operação > Orçamentos` o botão `Receber`, o estado/callback de recebimento e o `ModalRecebimento`; o detalhe agora mantém somente consulta de contas/boleto e impressão, sem baixa de pagamento. A implementação legada órfã `pages/recebimento.tsx` foi removida; o fluxo único permanece em `Caixa` (`ModalPedidoCaixa`). O endpoint backend continua protegido pela permissão `caixa.cadastrar`, com validação adicional que impede vendedor de receber o próprio pedido. Frontend validado com **45 testes**, `npm run typecheck` e `npm run build`; os testes backend direcionados não foram executados via `docker compose exec` porque o container está ligado ao banco persistente e não possui `TEST_PG_URL` isolado, evitando risco de recriar o schema do DEV. Nenhum deploy, staging ou produção foi acionado.

- **2026-09-02 (plano de classificação financeira e rateio)**: criado `docs/PLANO_CLASSIFICACAO_DESPESAS_RATEIO_PRECIFICACAO.md`, definindo a evolução do Plano de Contas para natureza/política de rateio, obrigatoriedade em Contas a Pagar, herança em compras, competência gerencial, rateios parciais, prevenção de dupla contagem, integração com o motor de precificação, RBAC, auditoria, migrations Expand/Migrate/Contract, sprints e testes. Nenhum código funcional, migration, restart ou deploy foi executado.

> **Regra para agentes (opencode, Codex e afins):**
> 1. **LEIA este arquivo + `AGENTS.md` no início de cada sessão.**
> 2. **ATUALIZE ao final de cada sessão**: log de implementações, tarefas pendentes e próximos passos.
> 3. Nunca invente estado — reflita o que está realmente commitado/deployado.

---

## 1. O que é o projeto

ERP/Catálogo da **Casa LM** (materiais elétricos, parafusos, ferramentas). Nome técnico do repo: `ecommerce_scraper`
(raiz: `C:\Users\jpsantos\Documents\Projetos\ecommerce_scraper`; remoto `https://github.com/janiosantos/sisCotacao`, branch `main`).

- **Backend**: Python 3.14 · Flask · PostgreSQL 16 · `psycopg` (com shim `pgsql` que emula `sqlite3`)
- **Frontend**: React + TypeScript + Vite + Tailwind (SPA em `frontend/`)
- **Infra**: Docker Compose + nginx (proxy `/api`, `/images`, impressões e portal)
- **Site institucional** (em paralelo): `C:\Users\jpsantos\Documents\Projetos\CASA_LM\site` — consumirá a **API pública** deste backend.

## 2. Regras de ouro (não negociáveis)

1. **DEPLOY para STAGING/PRODUÇÃO SOMENTE com confirmação explícita do usuário.** Commit/push é seguro; deploy não.
2. Migrações **versionadas** em `backend/migrations/versions/` com `RISCO` + `MUDANCA` (o_que/porque). `AUTO_MIGRATE=0` — aplicar por comando explícito.
3. Todo código vai para Git (`main`). Frontend **nunca** acessa o PostgreSQL diretamente (só a API).
4. Mudança incompatível de contrato → Expand/Migrate/Contract (ciclo A–F).
5. Testar: backend `pytest`, frontend `npm run typecheck` + `npm run build` + vitest.
6. Fonte da verdade de imagens de produto = **filesystem** `images/cadastro/<produto_id>/`; banco guarda só o caminho relativo.

## 3. Ambientes e acesso

| Ambiente | Onde | Frontend | Observações |
|---|---|---|---|
| DEV (notebook) | Docker local | `http://localhost:8080` | código bind-mount; migrações por comando |
| VM | `jpsantos@10.189.14.9` (`~/Projetos/ecommerce_scraper`) | `http://10.189.14.9:8080` | sudo `Mudar@123`; sempre `git pull` antes de operar |
| STAGING | servidor (projeto `siscom-staging`) | `:8081` | banco nasce vazio e é migrado pelo pipeline |
| PRODUÇÃO | servidor | domínio | só recebe release confirmada |

- Banco dev: `localhost:5432/catalog` (catalog/catalog) — mesmo banco do docker local.
- Banco de teste: `catalog_test` (env `TEST_PG_URL=postgresql+psycopg://catalog:catalog@localhost:5432/catalog_test`).
- **Atenção**: rodar o backend FORA do docker grava imagens em `backend/images` (errado). Usar `IMAGES_DIR=C:\Users\jpsantos\Documents\Projetos\ecommerce_scraper\images`.

## 4. Estado atual

- **Versão publicada (produção)**: `v2.32.2` — sem deploy das mudanças recentes (por decisão do usuário, produção só recebe release aprovada). **Staging** roda o pacote de hardening + P2/P5/P6.
- **Schema**: dev em **104** (migrações `0052..0104`): 0097 `cobranca_ambiente`, 0098 `login_rate_limit`, 0099 `webhook_secret`, 0100 `webhook_log`, 0101 `outbox`, 0102 `outbox_claim`, 0103 `rbac_auditoria_tokens`, 0104 `rbac_emitir_fiscal_operador`. Staging/produção aguardam release autorizada. A **0095 foi removida** (nunca commitada).
- **Hardening (Codex)**: RBAC deny-by-default (sem perfil → 403), gates por ação, segregação de desconto, proteção do último administrador, auditoria RBAC e revogação de tokens, além de `MAX_CONTENT_LENGTH`, `safe_http` (SSRF), rate limit de login (PostgreSQL, 5/300s), segredos de pagamento não vazam, `deploy.yml` passa `CATALOG_SECRET`/`POSTGRES_USER`/`POSTGRES_PASSWORD` (secrets do GitHub configurados). **248 testes** backend + **34 testes** frontend verdes.
- **Webhooks (P2)**: assinatura/token nativo por provedor (MP x-signature+anti-replay, Asaas access-token, EfiPay token), `webhook_log`, rechecagem (manual + periódica), baixa por notificação validada no staging (Asaas sandbox pix/boleto).
- **Outbox (P5)**: Redis + worker RQ + scheduler; tabela `outbox` com retry/backoff/dead-letter, upsert idempotente e claim/lease concorrente; webhook 503 enfileira rechecagem. Validação automatizada concluída em DEV; staging aguarda release.
- **TLS**: staging usa HTTP `:8081` e HTTPS `:444`, com o mesmo domínio/certificado da produção em volume somente-leitura; a separação é por projeto Docker e porta TCP. Produção continua na porta 443.
- **Frontend (P6)**: **29 telas modularizadas, 93 módulos**; `ApiError`/`mensagemErro`, cache curto de GET com invalidação, validação runtime mínima de contratos críticos e Error Boundary; 34 testes; mojibake 0 em todo o frontend.
- **Imagens**: 189.094 linhas em `imagens_produto`, 0 sem arquivo físico; arquivos em `images/cadastro/`. Dry-run P8: 3.188 sem imagem, 5.648 arquivos órfãos (435 MB), 2.742 pastas órfãs (1.060 MB) — aguarda aprovação.
- **Produtos**: ~62.731 em `produtos_cadastro`.
- **API pública**: `/api/publico/*` (produtos/categorias/marcas; paginação `offset/limit` + `has_more`; busca `?q=`; filtros). Sem token, CORS limitado por `PUBLIC_CORS_ORIGINS`.
- **OpenAPI**: `backend/openapi.json` (**66 paths**) servido em `/api/openapi.json`.

## 5. Log de implementações (recentes)

| Versão | O que foi feito |
|---|---|
| **v2.32.2** | Paginação explícita em `/api/publico/produtos` (`has_more`), busca `?q=`; correção da página demo (erro claro de JSON); AGENTS.md com regra de deploy. Deploy prod→staging (exceção). |
| **v2.34.0 (hardening)** | Manifesto criado/pushado; **validado em staging** (não em produção): RBAC deny-by-default, `MAX_CONTENT_LENGTH`, `safe_http` (SSRF), login rate limit (0098), segredos de pagamento ocultos. |
| **v2.35.0 (correções da auditoria)** | **P2/P5** — baixa conta+caixa na mesma transação, emissão concorrente protegida por lock e idempotência do gateway; **P5** — outbox com upsert, claim/lease e retry de payload inválido (0102); **P4/P6** — CORS allowlist, site sem catálogo demo no SSR e Error Boundary de rota. Validado localmente; sem deploy. |
| **sem release (staging via SSH)** | **P2** — webhooks com assinatura nativa por provedor (0099 `webhook_secret`, 0100 `webhook_log`), rechecagem manual/periódica, baixa por notificação validada (Asaas sandbox); **P5** — RQ+Redis (worker/scheduler) + outbox transacional (0101) com retry/backoff/dead-letter; **P6** — frontend modularizado (29 telas, 93 módulos) + `ApiError` + 27 testes. Aguarda release aprovada. |
| **v2.32.1** | Página demo `/demo-publico.html` (consome a API pública). |
| **v2.32.0** | API pública `/api/publico/*` (produtos/detalhe/categorias/marcas) + CORS + OpenAPI (56→60 paths). |
| **v2.31.0** | Imagens: `filename` relativo ao `IMAGES_DIR` (0093), padronização física em `cadastro/` (0094), **remoção da 0095** (nunca commitada), reconstrução de vínculos a partir do filesystem (0096, +81.305 linhas). 189.094 linhas, 0 quebradas. |
| **v2.30.0** | Imagens em lote por fornecedor (busca/preview/aplicar), dedup MD5, retry 1x, limites 20/20, favorita = capa. |
| **v2.29.x** | Busca: relevância por cobertura de palavras; spec distintivo no card (atributos+marca+unidade). |
| **v2.28.0** | Taxonomia grupos/subgrupos (0092), combobox categoria filtrado, `reclassificar_cabos`, `normalizar_subcategorias`. |
| **v2.27.0** | Descrição padronizada (nome+atributos+marca) + busca ILIKE/pg_trgm (0091; `produtos_fts` removido). |
| **v2.26.0** | Unificação produto/variante (0085–0090): variantes viraram produtos independentes; EAV `variante_atributos` dropado. |
| **sem release (RBAC hardening)** | Migrações `0103`/`0104`: auditoria e revogação de tokens, proteção transacional do último administrador, segregação de funções, ação fiscal `emitir`, autoria Bearer e gates de ações críticas. Validado localmente; sem deploy. |

## 6. Tarefas pendentes (priorizadas)

**Alta**
- [ ] **Ativar TLS/Let's Encrypt em PRODUÇÃO** (`siscom.casalm.com.br`): staging já usa HTTPS `:444` com o certificado compartilhado em somente-leitura; produção requer ajuste do roteamento para a 443 interna + `docker compose up -d --build` + confirmação do usuário — ver `deployment/certbot/LEIA.md`.
- [x] **Integrar o site institucional (código)** (`C:\Users\jpsantos\Documents\Projetos\CASA_LM\site` — repo `janiosantos/casa-lm-site`, Astro) à API pública: `api.ts` usa `PUBLIC_API_ORIGIN`, CORS do backend está em allowlist e o SSR não publica dados demonstrativos. Falta validação live após publicação.
- [ ] **Publicar em produção** o pacote hardening + P2/P5/P6 (deploy autorizado; produção segue em v2.32.2).

**Média**
- [ ] **P8 — saneamento de imagens**: dry-run commitado em `reports/*` (3.188 sem imagem; 5.648 arquivos órfãos/435 MB; 2.742 pastas órfãs/1.060 MB). **Aguardando aprovação do usuário** para lote por fornecedor + remoção de órfãos (com backup).
- [ ] Homologação **Focus NFe** com credenciais reais (NF-e/NFC-e) — adapters prontos em staging.
- [ ] Ligar **`FISCAL_ENGINE_V2` em PRODUÇÃO** (após deploy autorizado das releases 1.11→2.14).
- [ ] **P6 residual**: virtualizar tabelas extensas e testes E2E de fluxos críticos (PDV/estoque/financeiro/compras/fiscal). Cache e validação runtime mínima já entregues.
- [ ] Renomear pasta raiz para `casa-lm` (manual, fechar editores; o site já está em `CASA_LM/site`).

**Baixa / infra**
- [ ] Remover arquivos soltos `2.5mm²` sem linha em `images/cadastro/62455` e `62461` (restos de teste).
- [ ] Reativar a fila do webhook no Asaas sandbox (após 15 falhas) e revalidar entrega real (P2).
- [ ] Ampliar cobertura de testes frontend (vitest — hoje 34) e E2E de fluxos críticos.

## 7. Próximos passos sugeridos

1. **P8**: aprovar o lote de imagens por fornecedor + remoção de órfãos (dry-run pronto em `reports/*`).
2. **P6 residual**: virtualização de tabelas e testes E2E.
3. **P4**: apontar `siteConfig.ts` do `CASA_LM/site` para `/api/publico/*` + restringir CORS.
4. **Publicação autorizada**: release hardening + P2/P5/P6 em produção (produção segue em v2.32.2).
5. **P3 fiscal**: desbloquear com certificado A1/A3 + contador (homologação Focus/SEFAZ).

## 8. Convenções de trabalho para agentes

- **Começar**: ler `AGENTS.md` + `CONTEXTO_SESSAO.md` + `DESENVOLVIMENTO.md` (histórico técnico) + `git log --oneline -15`.
- **Análise de bugs (Codex)**: usar o checklist `TODO.md` — registrar o resultado na seção final.
- **Terminar**: atualizar `CONTEXTO_SESSAO.md` (log + pendências + próximos passos) e commit/push.
- **Backend**: após alterar, `python -m py_compile <arquivo>`; testes `pytest` (env `TEST_PG_URL` apontando para `catalog_test`). 215 testes verdes hoje.
- **Frontend**: `npm run typecheck` e `npm run build` (a partir de `frontend/`); testes `npm test`.
- **Migração nova**: arquivo `NNNN_*.py` em `backend/migrations/versions/` com `VERSION`, `RISCO`, `NAME`, `MUDANCA`, `guard`, `forward`, `backward`. Aplicar no dev com `python -m catalog_server.versioning apply --origem local`. Testar banco vazio→head (o CI do staging faz isso).
- **Deploy**: NUNCA sem confirmação explícita do usuário (ver AGENTS.md). Apresentar resumo e aguardar.
- **Manter os dois ambientes em sincronia** (notebook ↔ VM) via git pull/push.
- **Backend dev (Docker) NÃO tem reloader** (`CATALOG_DEBUG` default 0): após alterar código backend, **`docker compose restart backend`** para carregar as rotas/serviços novos (o código é bind-mount; só o processo precisa reiniciar). Testar via `http://127.0.0.1:8080` (o `localhost:8080` é sequestrado pelo wslrelay no `[::1]`).

## 9. Registro da sessão atual

- **2026-09-01 (auditoria do Plano Mestre):** revisado o trabalho das ondas até a migração **0142**, confrontando implementação com os critérios de aceite de `PLANO_MESTRE_IMPLEMENTACAO_ERP.md`. Criado `RELATORIO_AUDITORIA_PLANO_MESTRE_ERP.md` e atualizado `TODO.md`. Foram confirmados bloqueios P0/P1 em ABC histórica (consumo/depósito/aplicação global), recebimento financeiro (condição/duplicidade/erro contábil), conciliação bancária (conta a pagar atualizada como receber), autoria Bearer em APIs de compras/recebimento, motor de reposição por depósito, idempotência central, relatórios/OpenAPI e pipeline XML→recebimento. **Nenhuma correção de código, migração, restart ou deploy foi executado nesta sessão; o objetivo foi auditar e registrar as pendências.** Frontend: `typecheck` + 39 testes verdes. Backend direcionado: 37 testes verdes em 5m23s; suíte integral não declarada aprovada.

- **2026-09-01 (infra — timezone America/Sao_Paulo):** `TZ=America/Sao_Paulo` em db/backend/worker/scheduler/vite/frontend no `docker-compose.yml`; `backend/Dockerfile` + `tzdata` + `ENV TZ`; PostgreSQL com `command: postgres -c timezone=America/Sao_Paulo` (o `TZ` do SO não muda o GUC do PG). Confirmado: `SHOW timezone` → `America/Sao_Paulo`; `date` → `-03` nos containers. Stack healthy (health 200). Commit `3e2871c`. Sem deploy.

- **2026-09-01 (Onda 10 — UX, continuação autônoma):**
  - **UX-001** `PageHeader` ganhou `contexto` (rótulo do grupo do shell); aplicado em Clientes.
  - **UX-002/006** novo componente `Paginacao` (total + nav acessível com `aria-label`, anterior/próxima) aplicado no **Saldo de Estoque** (50/página).
  - **UX-007** `aria-sort`/`aria-label` na ordenação da tabela; foco-visible e `prefers-reduced-motion` já presentes no CSS.
  - Modal já cobria UX-003 (Escape, trap, aria-modal, restauração de foco).
  - Testes frontend **37 → 39** (ux.test.tsx: PageHeader.contexto + Paginacao). Sem deploy.

- **2026-09-01 (Onda 10 — UX):** (feita por último, como instruído — layout será reajustado em conjunto)
  - **UX-001** shell/navegação: menu reorganizado nos grupos do plano — **Operação / Comercial / Compras / Financeiro / Fiscal / Relatórios / Administração**.
  - **UX-002** tabela padrão: `THead` ganhou **ordenação clicável** (botão com `aria-label`, `aria-sort`, indicador ▲▼) sem quebrar o `data-label` do mobile card.
  - **UX-005/007** estados e acessibilidade: testes de `aria-sort`, rótulos e preservação de `data-label` (renderToStaticMarkup).
  - Testes frontend **34 → 37** (ux.test.tsx). Sem deploy.

- **2026-09-01 (INT-003/005 + ADM-004 + ARC-001):**
  - **INT-003** impressão: `reenfileirar` (reimpressão/retry **auditada** via `auditoria_evento`; falha não perde a venda) + `POST /api/impressao/fila/<id>/reimprimir`.
  - **INT-005** transporte/entrega (migração **0142**): `transportadora` (nome/CNPJ/telefone/prazo médio), `expedicao_evento` (eventos logísticos), `expedicao` + transportadora_id/sla_data/data_envio/data_entrega/rastreio; status logístico (`pendente/planejada/separada/enviada/parcialmente_entregue/entregue/cancelada`) **separado do fiscal/financeiro**; SLA = hoje + prazo da transportadora; entrega parcial possível. API `/api/estoque/transportadoras`, `/expedicao/<id>/transporte|status|eventos`.
  - **ADM-004** backup: `scripts/backup.py` (pg_dump schema+data + imagens + manifest SHA-256 + retenção N) + `docs/erp/backup.md` (RPO/RTO, restauração isolada, evidência).
  - **ARC-001** services/use cases: teste de que as regras importam e rodam **sem Flask** + ponto transacional único (finalizar do recebimento).
  - Testes `test_transporte_impressao.py` (6) + `test_arc_001_backup.py` (4). Schema DEV **142**. Sem deploy. Próximo: **Onda 10 — UX** (por último, pois o layout será reajustado).

- **2026-09-01 (Ondas 10/11 restantes + Piloto):**
  - **ARC-002** validação de schema (`services/validacao.py`): tipos/enum/decimal/datas/limites/campos desconhecidos; aplicado ao `importar_extrato`.
  - **ARC-004** concorrência real: testes de **último saldo serializado** (advisory lock — saldo nunca negativo) e idempotência concorrente (`test_arc.py`).
  - **ARC-007** migrações: teste do guard idempotente + schema aplicado.
  - **INT-002** cobrança/adquirentes: `status_canonico` + `reconciliar_pendentes` (cobranças pendentes/erro com provider e ação p/ sem-provider).
  - **ADM-001** carga inicial (`services/operacao.py`): importa clientes/fornecedores **idempotente por doc**, rejeições listadas.
  - **ADM-002** deduplicação: `candidatos` (SKU/EAN/CPF/CNPJ duplicados) + `merge` assistido (**redireciona referências**, **não destrói documento**, auditado).
  - **ADM-005** monitoramento: `GET /api/sistema/readiness` (banco, migrações pendentes, outbox morta; 503 se não pronto).
  - **PIL-001..004**: `docs/erp/piloto.md` com checklist P0, evidências, ações pré-publicação e aprovação explícita.
  - Testes `test_arc.py` (6) + `test_operacao.py` (8). Sem deploy.

- **2026-09-01 (Ondas 10/11 — ARC-003/005/006 + INT-001/006 + ADM-003):** migração **0141** (`idempotencia`, `auditoria_evento`, `conta_bancaria`, `extrato_bancario`).
  - **ARC-003** idempotência central (`services/infra.py`): `executar(chave, escopo, payload, fn)` — retry devolve resultado anterior; **chave com payload diferente é rejeitada**.
  - **ARC-005** reconciliação (`services/reconciliacao.py`): divergências com severidade/origem/ação (pedido sem movimento, movimento sem origem, **saldo inconsistente** vs ledger, reserva órfã, outbox morta).
  - **ARC-006** auditoria (`infra.registrar`): ator/acação/alvo/antes-depois **mascarados (LGPD)**/motivo/IP/correlation_id.
  - **INT-001** conciliação bancária (`services/conciliacao.py`): importa extrato **idempotente**, **sugere matching** por valor/documento, **aprova baixa a conta** (nunca automática sem regra), rejeita — auditado.
  - **INT-006** comunicação (`services/comunicacao.py`): agenda e-mail/whatsapp no **outbox** (retry/backoff/dead-letter; envio não bloqueia o fluxo; destinatário mascarado).
  - **ADM-003 LGPD** (`services/lgpd.py`): máscara de CPF/CNPJ/e-mail/telefone/senhas em logs e auditoria.
  - API `/api/infra/{reconciliacao, auditoria, contas-bancarias, movimentos..., comunicacao}` (RBAC configuracoes). Testes `test_infra.py` (10). Schema DEV **141**. Sem deploy. Suíte completa (~505) a validar em background.

- **2026-09-01 (Onda 9 — POS-001..005):** migrações **0139** e **0140**.
  - **POS-001/002** (`services/posvenda.py`): `rma` (estados solicitada→autorizada→recebida→analisada→concluída/rejeitada; **devolução acima do vendido bloqueada**; prazo/motivo/condição/lote), `troca` (item substituto + **diferença** → crédito/estorno), `credito_cliente` (origem única — **não duplica**). `_concluir_efeitos` repõe estoque + gera crédito do cliente.
  - **POS-003**: `garantia` + fornecedor_id/n_serie/laudo/custo/responsabilidade/sla_data.
  - **POS-004** (`services/crm_comissao.py`): `oportunidade` (etapas, próxima ação/contato, **motivo de perda obrigatório**, carteira filtrável).
  - **POS-005**: `comissao` + `comissao_politica` (política **versionada**); `apurar_venda` (base = venda líquida, % congelado — DECISAO-006), `reverter` (**estorno/devolução gera reversão, não edição retroativa**), `listar`.
  - API `/api/posvenda/{rma, rma/<id>/status, rma/<id>/trocar, credito/<cliente>, oportunidades, comissoes/apurar/<orc>, comissoes/reverter/<orc>, comissoes}` (endpoints legados interações/garantia preservados). Testes `test_posvenda.py` (6) + `test_crm_comissao.py` (5). Schema DEV **140**. **Onda 9 concluída.** Sem deploy. Suíte completa (~495) a validar em background. Próximo: Onda 10 (UX/ARC) + Onda 11 (INT/ADM) + Onda 12 (Piloto).

- **2026-09-01 (Onda 8 — BI-001..007 relatórios/indicadores):** `services/relatorios.py` (camada de consultas analíticas separada da operacional):
  - **BI-002 dashboard executivo**: pedidos, receita bruta/líquida, desconto, **CMV** (custo histórico do ledger), **margem**, ticket médio, caixa, **inadimplência**, estoque valorizado, compras em aberto.
  - **BI-003 vendas**: agrupável (produto/marca/grupo/vendedor/cliente/depósito/canal/forma), **cancelados/devolvidos separados**, receita bruta/líquida + CMV.
  - **BI-004 compras**: pedidos/recebidos/cancelados, **lead time médio**, comprado.
  - **BI-005 estoque**: saldo/kardex por depósito, valorização (custo médio), **ruptura**, ABC/XYZ.
  - **BI-006 financeiro/DRE**: fluxo de caixa, **aging receber**, DRE (receita−CMV=lucro bruto).
  - **BI-007 central**: `#/relatorios` (App+rota+sidebar) com dashboard + abas Vendas/Compras/Estoque/Financeiro.
  - **BI-001**: agregações a partir do ledger/documentos (sem gravar em transação operacional); snapshot de período explícito.
  - API `/api/relatorios/{central,dashboard,vendas,compras,estoque,financeiro}` (endpoints legados vendas-periodo/aging/dre/margem-vendas preservados). Testes `test_relatorios.py` (7). Frontend: client + página. **Onda 8 concluída.** Sem deploy. Próximo: **Onda 9 — POS-001..005** (RMA, troca/crédito, garantia, CRM, comissões).

- **2026-09-01 (Onda 6 — VEN-006 cobrança e renegociação):** migração **0138** (`contas_receber` + juros_multa/cobranca_recalculada_em/renegociada_de; `config_cobranca` (juros_dia_pct, multa_pct); status + 'renegociada'). Service `services/cobranca.py`: `calcular_cobranca` (juros/multa de parcela **vencida** com política configurável — juros 0,033%/dia + multa 2%; registrado na conta), `renegociar` (cancela original → **novas parcelas** com vencimentos e juros/multa incorporados, origem renegociacao, vínculo renegociada_de, parcela i/N), `listar_vencidas` (cobrança), `atualizar_config`. API `/api/financeiro/cobranca/vencidas`, `/<conta>/recalcular`, `/renegociar`, `/config`. Testes `test_cobranca.py` (6). Schema DEV **138**. **Onda 6 (VEN) concluída e adequada ao documento.** Sem deploy. Próximo: **Onda 8 — BI-001..007** (relatórios/dashboard).

- **2026-09-01 (Onda 6 — VEN-003 pagamentos por pedido com entidade):** migração **0137** (`orcamento_pagamento`: forma, valor, taxa, provedor, bandeira, código de autorização, status pendente/confirmado/estornado, idempotency_key única por orçamento). Service `services/pagamento_venda.py`: `registrar` (valida soma ≤ total; **troco só em dinheiro**; **cartão/PIX ficam pendentes** — pagamento pendente não marca venda como paga; **retry não duplica** via idempotency_key), `confirmar` (confirma pendentes + lança caixa; troco em dinheiro; marca `recebido` quando soma fecha o total), `estornar` (**reverte caixa** e volta a venda para `finalizado` se ficou sem quitação), `listar`. API `/api/orcamentos/<id>/pagamentos` POST/GET, `/pagamentos/confirmar`, `/api/orcamentos/pagamentos/<id>/estornar`. Testes `test_pagamento_venda.py` (7). Schema DEV **137**. Sem deploy. Próximo: VEN-006 (renegociação/juros/multa) + Onda 8 BI + Onda 9 POS.

- **2026-09-01 (Onda 6 — VEN-001 unidade de venda; VEN-005 retirada/entrega):** migração **0136** (`orcamento_itens` + `unidade_vendida`/`fator_venda`; `orcamentos` + `tipo_entrega` (balcao/entrega), `status_entrega` (pendente/enviada/entregue), `endereco_entrega`, `data_entrega`). `orcamento_repo.substituir_itens` converte a **unidade de venda** (UN/CX/RL) para a unidade base via `unidade_conversao` (VEN-001 — venda fracionada não cria saldo inválido). Service `services/venda_entrega.py`: `configurar_entrega` (balcão ou entrega com endereço obrigatório; só após finalizar), `transicionar` (pendente→enviada→entregue; **entrega exige envio**; balcão usa `retirar` direto), `listar`. API `/api/orcamentos/<id>/entrega`, `/entrega/status`, `/api/orcamentos/entregas`. Testes `test_venda_unidade_entrega.py` (6: conversão CX→base, configurar+enviar+entregar, balcão retira, endereço obrigatório, sem pulo, API). Schema DEV **136**. Sem deploy.

**Onda 6 (VEN) status:** VEN-001 ✅ · VEN-002 ✅ (busca rápida já existente) · VEN-003 ✅ (pagamentos múltiplos já existentes no `receber`) · VEN-004 ✅ (sessão de caixa, `5add09b`) · VEN-005 ✅ · VEN-006 ✅ (crédito/parcelas já existentes) · VEN-007 = **manual no piloto (DECISAO-004)**. Próximo: Onda 7 — FIS (externo: matriz fiscal aprovada) / Onda 8 — BI.

- **2026-09-01 (Onda 6 — VEN-004 sessão de caixa):** migração **0135** (`caixa_sessao`: depósito, operador, terminal, saldo_inicial, status aberta/fechada, saldo esperado/contado/diferença, justificativa, aprovação; índice único parcial `operador_id WHERE status='aberta'`; `caixa_movimento` + `sessao_id`). Service `services/caixa_sessao.py`: `abrir` (1 sessão aberta por operador; registra abertura), `suprimento`/`sangria`, `fechar` (saldo esperado = saldo_inicial + entradas/suprimentos − saídas/sangrias da sessão; **diferença**; **bloqueia novos movimentos**), `aprovar` (diferença), `detalhe`/`listar`. `caixa_repo.movimentar` ganhou `sessao_id` (auto-vínculo à sessão aberta do operador; **bloqueia quando a sessão do operador está fechada** — deve abrir nova). API `/api/financeiro/caixa/sessao` POST abrir, `<id>/suprimento|sangria|fechar|aprovar`, GET listar/detalhe. Testes `test_caixa_sessao.py` (6). **VEN-003 (pagamentos múltiplos) já existia** no `receber` do orçamento (formas combinadas, troco só dinheiro, baixa por documento). Schema DEV **135**. Sem deploy. Próximo: VEN-001 (unidade de venda) + VEN-005 (retirada/entrega).

- **2026-09-01 (Onda 5 — REC-005 postagem transacional; REC-006 devolução):**
  - **REC-005**: migração **0133** (`recebimento_postagem`: registro de estoque_itens/contas_criadas/total/pedido_status/contabil_ok; UNIQUE por recebimento). `finalizar` agora é **transacional com lock** (`SELECT ... FOR UPDATE` no pedido), **idempotente** (já postado → devolve resultado gravado, `duplicado=True`, sem re-postar) e **dispara gatilho contábil** (`contabil_gatilhos.disparar('recebimento_compra')`, nunca trava a postagem). Erro desfaz tudo localmente.
  - **REC-006**: migração **0134** (`devolucao_fornecedor` + `_item`: recebimento, pedido, fornecedor, documento fiscal, motivo (avariado/erro_quantidade/nao_conforme/devolucao_comercial/outro), estado, status aberta/concluida/cancelada; itens com **lote rastreado**). Service `services/devolucao.py`: `criar` (**não devolve mais que o recebido**; valida motivo/estado; lote pertence ao produto; reserva quantidade devolvida), `concluir` (saída de estoque via `movimentar_fato` com lote + idempotency; **crédito/estorno a pagar** (conta negativa origem devolucao_fornecedor); auditoria), `cancelar`, `listar`/`detalhe`. API `/api/compras/recebimentos/<id>/devolucoes`, `/api/compras/devolucoes` (+`/<id>`, `/concluir`, `/cancelar`).
  - Testes `test_devolucao_recebimento.py` (7: finalizar idempotente, postagem auditada, devolução+concluir com crédito/estoque, não devolve mais, requer finalizado, lote rastreado, API). Schema DEV **134**. **Onda 5 (REC-001..006) concluída.** Sem deploy. Próximo: Onda 6 — VEN-001..007 (venda/PDV/caixa/pagamentos).

- **2026-09-01 (Onda 5 — REC-004 entrada fiscal XML):** migração **0132** estende a tabela legada `nfe_entrada` (chave_acesso única, fornecedor_id, protocolo, status importado/vinculado/confirmado/rejeitado, valor_total, emissao) + cria `nfe_entrada_item` (produto vinculável, código fornecedor, EAN, descrição, NCM/CFOP/CST, unidade, quantidade, valor unitário, status sem_vinculo/vinculado). Service `services/nfe_entrada.py`: `importar_xml` (parse XML NF-e com namespaces; valida chave 44 dígitos; **XML duplicado rejeitado**; extrai fornecedor por CNPJ; extrai itens com impostos/CST; **matching por código fornecedor → EAN → descrição**), `vincular_item` (fiscal corrige vínculo antes da confirmação), `confirmar` (**item sem vínculo não confirma**; devolve linhas normalizadas p/ três vias), `rejeitar`, `listar`/`detalhe`. API `/api/nfe-entrada` POST/GET, `<id>`, `/itens/<id>/vincular`, `<id>/confirmar`, `<id>/rejeitar`. Testes `test_nfe_entrada.py` (7: importa+EAN, duplicado, sem vínculo não confirma, vínculo manual+confirmar, XML inválido, rejeitar, API). Schema DEV **132**. Sem deploy. Próximo: REC-005 (postagem transacional idempotente) + REC-006 (devolução ao fornecedor).

- **2026-09-01 (Onda 5 — REC-003 três vias):** migração **0131** (`recebimento` + status_tres_vias aguardando_conferencia/divergente/aprovado/rejeitado + divergencia_aprovada_por/em/rejeitada_motivo; `recebimento_divergencia` por produto/tipo quantidade|preco|fiscal com dif_pct e dentro_tolerancia). Service `services/tres_vias.py`: `conferir` (compara NF × pedido, aplica **tolerâncias do fornecedor** (`tolerancias_compra`), grava divergências; sem divergência → aprovado; fora da tolerância → **divergente**), `aprovar` (só aprovação libera efeitos definitivos), `rejeitar` (motivo), `divergencias`. **Gate no `finalizar`**: divergente/rejeitado bloqueiam até aprovação. API `/api/compras/recebimentos/<id>/tres-vias`, `/divergencias`, `/tres-vias/aprovar`, `/tres-vias/rejeitar`. Testes `test_tres_vias.py` (7: sem divergência aprovado, dentro da tolerância, fora exige aprovação (finalizar bloqueia), preço, item sem pedido, rejeitar, API). Schema DEV **131**. Sem deploy. Próximo: REC-004 (entrada fiscal XML) + REC-005 (postagem transacional) + REC-006 (devolução ao fornecedor).

- **2026-09-01 (Onda 5 — REC-002 conferência por código/unidade):** migração **0130** (`recebimento_item` + `codigo_conferido`, `unidade_conferida`). Service `services/conferencia.py`: `resolver_codigo` (busca exata via `produto_identificador`/SKU/EAN/código fornecedor → produto + unidade base + fator; **produto desconhecido → None/exceção**), `converter_para_base` (via `unidade_conversao` — caixa/rolo não gera quantidade errada), `conferir_por_codigo` (resolver → converter → conferir o item → **divergência** esperado×conferido, registra código/unidade conferidos). API `/api/compras/recebimentos/<id>/itens/scanner` + `/api/compras/conferencia/resolver`. Testes `test_conferencia_recebimento.py` (6: resolver SKU/EAN, desconhecido, caixa converte CX→UN, divergência, fora da lista, API). Schema DEV **130**. Sem deploy. Próximo: REC-003 (três vias pedido×recebimento×NF) e REC-004 (entrada fiscal XML).

- **2026-09-01 (Onda 5 — REC-001 documento de recebimento):** migração **0129** cria `recebimento` (cabeçalho: pedido, fornecedor, depósito, operador, documento fiscal, data, status aberto/conferido/finalizado/cancelado; UNIQUE pedido+documento → **retry não duplica**) e `recebimento_item` (pedido_item, produto, qtd pedido/recebida/aceita/recusada/avariada, status pendente/recebido/aceito/recusado/avariado). Service `services/recebimento.py`: `criar` (a partir do pedido; rejeita pedido recebido/cancelado; retry idempotente), `conferir_item` (**conferência parcial**; valida que a soma dos recebimentos (finalizado+conferido) não **excede o saldo do pedido**; marca conferido quando todos os itens conferidos), `finalizar` (entrada de estoque só dos **aceitos** via `movimentar_fato` com custo do pedido; contas a pagar 1×30d (origem pedido_compra); **status do pedido → recebido/parcialmente_recebido**), `detalhe`/`listar`. API `/api/compras/pedidos/<id>/recebimentos`, `/api/compras/recebimentos` (+`/<id>`, `/itens/<id>/conferir`, `/finalizar`). Testes `test_recebimento_documento.py` (6: retry idempotente, conferência parcial não excede, finalizar estoque+contas, pedido parcial→recebido, cancelado rejeita, API). Schema DEV **129**. Sem deploy. Próximo: REC-002 (conferência por código/unidade), REC-003 (três vias), REC-004..006.

- **2026-09-01 (Onda 4 — COM-010 alçada de compra; COM-011 pedido; COM-012 histórico):**
  - **COM-010**: migração **0127** (`alcada_compra`: limites por perfil/fornecedor/centro de custo, exige_aprovacao; `alcada_aprovacao`: aprovado/rejeitado/invalidado com motivo, antes/depois JSONB, versão). Service `alcada_compra.py`: `limite_usuario` (via perfis, filtros), `precisa_aprovacao`, `registrar_aprovacao`/`invalidar_aprovacao` (**alteração invalida aprovação**), `aprovado_vigente`, `criar_regra`/`listar_regras`. API `/api/alcada-compra` GET/POST, `/verificar`, `/aprovacoes`. Mapeado no RBAC (`/api/alcada-compra` → compras).
  - **COM-011**: migração **0128** (CHECK status: rascunho→aprovado→enviado→confirmado→parcialmente_recebido→recebido, cancelado; data_pedido). Service `pedido_compra.py`: `gerar_pedido` (**um pedido por fornecedor vencedor** da cotação; exige decisão concluída), `transicionar` (máquina controlada), `cancelar` (**recebido/parcial não cancela**; auditado na observação), `pode_receber`. API `/api/cotacoes/<id>/gerar-pedido`, `/api/compras/pedidos/<id>/status`, `/cancelar`.
  - **COM-012**: `historico_produto` (preços/prazos de pedidos + preferenciais por fornecedor) — API `/api/compras/historico`.
  - Testes `test_alcada_compra.py` (5) + `test_pedido_compra_estados.py` (6). Schema DEV **128**. Sem deploy. **Onda 4 (COM-007..012) concluída.** Próximo: Onda 5 — REC-001..006 (recebimento/nota de entrada/custo).

- **2026-09-01 (Onda 4 — COM-008 cotação por necessidade; COM-009 comparação):**
  - **COM-008**: migração **0125** (`cotacoes` + `solicitacao_id`, `versao`; `cotacao_itens` + `solicitacao_item_id`). Service `cotacao_necessidade.py`: `gerar_cotacao(sc_id)` idempotente (reusa cotação existente; valida status aprovada/cotando; **consolida itens sem duplicar**, preserva unidade; **convida fornecedores preferenciais** por ranking; marca solicitação → cotando), `buscar_propostas_por_produto` (itens sem proposta). API `/api/solicitacoes-compra/<id>/cotar`, `/api/cotacoes/<id>/propostas`.
  - **COM-009**: migração **0126** (`cotacao_precos` + vencedor/justificativa_vencedor/data_decisao/decidido_por; `cotacoes` + decisao_concluida). Service `comparacao.py`: `montar_comparacao` (matriz + **custo_efetivo** = preço líquido + impostos do motor fiscal por dentro + frete; **não escolhe só pelo menor preço**), `decidir_vencedor` (limpa vencedores do mesmo item; justificativa obrigatória; marca decisao_concluida quando todos os itens têm vencedor). API `/api/cotacoes/<id>/comparacao`, `/api/cotacoes/precos/<id>/vencedor`.
  - Testes `test_cotacao_necessidade.py` (6) + `test_comparacao.py` (5). Schema DEV **126**. Sem deploy. Próximo: COM-010 (alçada de compra) + COM-011 (pedido) + COM-012 (histórico).

- **2026-09-01 (Onda 4 — COM-007 solicitação de compra):** migrações **0123** (`solicitacao_compra` + prioridade/origem/centro_custo/deposito_id/prazo_desejado/data_enviada; `solicitacao_itens` + unidade/necessidade/origem_sugestao) e **0124** (amplia CHECK status p/ máquina rascunho→enviada→aprovada→cotando→convertida→cancelada). `SolicitacaoRepository` ganhou `transicionar` (máquina com transições válidas; **aprovador ≠ solicitante**; registra data/aprovador; `enviada` seta data_enviada), `_garantir_editavel` (**aprovada+ não edita itens** — bloqueia add/remover sem nova versão), `create` com campos novos, `add_item` com unidade/necessidade/origem_sugestao, `remover_item`. API `/api/solicitacoes-compra/<id>/transicao`, `DELETE .../itens/<item_id>`, POST ampliado. Testes `test_solicitacao_estados.py` (8: campos novos, transição completa, inválida, aprovador=solicitante, aprovada não edita, remover rascunho, cancelada, API). Schema DEV **124**. Também corrigido `abc_historica._resumo_por_classe` (float+Decimal → 2 falhas da suíte completa). Sem deploy. Próximo: COM-008 (cotação a partir de necessidade) + COM-009 (comparação) + COM-010 (aprovação) + COM-011 (pedido) + COM-012 (histórico).

- **2026-09-01 (Onda 3 — COM-006 tela de sugestões):** aba **"Sugestões de compra"** no módulo Compras (`compras/necessidades.tsx`): motor de reposição com filtros **depósito/ABC/XYZ/fornecedor/ruptura**, colunas selecionáveis, **detalhe do cálculo** (todos os componentes + justificativa), **seleção em lote** (selecionar necessidades), **ajuste por linha** (quantidade + justificativa), **"Gerar solicitação"** (cria `solicitacao_compra` + itens via API existente; ação respeita RBAC do gate). O motor passou a expor `classe_abc`/`classe_xyz`. Compras carrega depósitos p/ filtro. Frontend 34/typecheck/build ✓. Motor tests 6 ✓. Sem deploy.

**Onda 3 (COM-001..006) concluída.** Próximo: **Onda 4 — Compras COM-007..012** (solicitação, cotação por necessidade, comparação, aprovação, pedido, histórico). Suíte completa (esperado ~384) a validar em background.

- **2026-09-01 (Onda 3 — COM-005 desempenho do fornecedor):** migrações **0121** (`fornecedor_desempenho`: janela, n_pedidos, lead_time_medio/desvio, fill_rate, preco_liquido_medio, indisponibilidade, atraso, confiança alta/media/baixa; `fornecedores` + `lead_time_override` + `motivo`) e **0122** (`pedidos_compra` + `data_prometida/enviada/recebida/aceita`). Service `services/fornecedor_desempenho.py`: `calcular` (lead time real = recebida−geração; atraso = recebida−prometida; fill rate = recebidos/n; confiança por amostra ≥5 alta; **lead_time_efetivo = override > real (confiança≠baixa) > prazo contratual**), `set_override` (com motivo), `historico`. API `/api/estoque/fornecedores/<id>/desempenho`, `/desempenho/historico`, `/lead-time` PUT. OpenAPI 111→**114 paths**. Testes `test_fornecedor_desempenho.py` (5). Schema DEV **122**. Frontend: client methods. Sem deploy. Próximo: **COM-006 — tela de sugestões de compra** `#/compras/necessidades` (consumirá ABC/XYZ/motor/fornecedor).

- **2026-09-01 (Onda 3 — COM-004 motor de reposição):** migração **0120** (política `sob_encomenda` no `estoque_parametro`). Service `services/motor_reposicao.py`: `calcular(produto_id, deposito_id)` — disponível projetado = físico − reservado − bloqueado − separação + **compras confirmadas em trânsito** (pedidos não recebidos/cancelados) − **demanda aberta** (orçamentos em_análise/liberado não-virou_pedido + demanda_registro aberta); necessidade = max(0, alvo + demanda_lead_time − disponível_projetado); **data provável de ruptura** (disponível/média diária); arredondamento por **lote múltiplo/mínimo/máximo** (respeita unidade); **não duplica** quando o trânsito já cobre; **sob encomenda não vira estoque**; usa `estoque_parametro.obter_efetivo` + `fornecedor_preferencial` (ranking, ultimo_preco, prazo_entrega_dias). Inclui produtos com demanda mesmo sem `estoque_saldo`. API `GET /api/estoque/reposicao`. OpenAPI 110→**111 paths**. Testes `test_motor_reposicao.py` (6: necessidade sem estoque, trânsito evita duplicar, ruptura provável, lote múltiplo, sob encomenda, API). Schema DEV **120**. Frontend: client `ReposicaoResultado` (UI completa no COM-006). Sem deploy. Próximo: COM-005 (fornecedor/lead time real) + COM-006 (tela de sugestões `#/compras/necessidades`).

- **2026-09-01 (Onda 3 — COM-002 XYZ + matriz; COM-003 base de demanda):** migrações **0118** e **0119**.
  - **COM-002**: `produtos_cadastro` + `classe_xyz/cv_demanda/intermitente/xyz_calculado_em`; `xyz_config` (limiares cv_x/cv_y, meses_historico, intermitente_zeros_pct — configuráveis/auditados, UPSERT). Service `services/xyz.py`: `classificar` (média/desvio/CV sobre vendas mensais finalizadas nos últimos N meses; intermitente se % zeros ≥ limiar; X/Y/Z), `calcular_todos`, `matriz_politica` (ABC×XYZ → política de estoque/contagem/serviço), `resumo_matriz`, `atualizar_config`. API `/api/estoque/xyz/calcular`, `/xyz/config` GET/PUT, `/xyz/matriz`.
  - **COM-003**: `demanda_registro` (produto, data, quantidade, tipo real/projetada, origem venda|pedido_aberto|reserva|devolucao|obra|projeto|manual, status atendida|perdida|aberta, motivo_ruptura; UNIQUE origem+origem_id+produto). Service `services/demanda.py`: `consolidar` (idempotente — **pedido cancelado não entra**; vendas finalizadas), `registrar_manual` (chave → origem_id via crc32 p/ idempotência), `marcar_perdida` (ruptura com motivo), `listar`, `auditar` (atendida/perdida/aberta + documento do orçamento). API `/api/estoque/demanda/consolidar`, `/demanda` POST/GET, `/demanda/auditar/<produto_id>`, `/demanda/<id>/perdida`.
  - OpenAPI 103→**110 paths**. Testes `test_xyz_politica.py` (5) + `test_base_demanda.py` (6). Frontend: client methods (XYZ/demanda); UI completa das sugestões chega no COM-006. Schema DEV **119**. Sem deploy. Próximo: **COM-004 motor de reposição** + COM-005 (fornecedor/lead time) + COM-006 (tela de sugestões).

- **2026-09-01 (Onda 3 — COM-001 ABC histórica):** migração **0117** cria `abc_calculo` (versão: critério consumo/receita/margem/quantidade/frequência, período, depósito, parâmetros/cortes JSONB, origem historico|bootstrap, total, acumulado) e `abc_calculo_item` (produto, valor, acumulado, pct_acumulado, classe, ordem; UNIQUE calculo+produto); `produtos_cadastro` + `abc_origem` (historico|bootstrap). Service `services/abc_historica.py`: `calcular` (métricas do ledger + vendas finalizadas — **exclui cancelado**; receita=Σqtd×preço; margem=receita−custo do movimento; frequência=distinct dias; classificação A/B/C por acumulado 70/90), `aplicar` (grava classe/ordem + abc_origem='historico' só nos com venda), `listar`/`detalhe`. API `/api/estoque/abc` POST calcular, `<id>/aplicar`, GET listar/detalhe. OpenAPI 99→**103 paths**. Frontend: aba **ABC** no Estoque (`estoque/abc.tsx`: calcular por período/critério, versões, aplicar, detalhe com badges A/B/C). Testes `test_abc_historica.py` (8: exclui cancelado/fora período, receita/margem, classificação acumulada, sem-venda separado, validações, aplicar marca origem, listar/detalhe, API). Schema DEV **117**. Sem deploy. Próximo: COM-002 (XYZ) e COM-003 (base de demanda).

- **2026-09-01 (Onda 2 — EST-008 rastreabilidade de lote):** migração **0116**: `lotes` + status (ativo/bloqueado), origem (compra/producao/avulsa), custo_unitario, fornecedor_id, documento, observacao; `familias` + `controle_lote` (DECISAO-011: lote parametrizado por família); índice por validade. Service `services/lote_rastreabilidade.py`: `criar_lote` (entrada do lote no ledger na MESMA transação — `_conn`; corrigido bug `cur.lastrowid`→`.fetchone()["id"]` para INSERT com RETURNING), `familia_controla_lote`, `status_derivado` (vencido/bloqueado/ativo), `atualizar_status`, `fefo` (FEFO: primeiro a vencer primeiro a sair; exclui vencido/bloqueado; `lotes.quantidade` agora é debitada na saída com lote), `recall` (clientes/documentos afetados via ledger: saída com lote → orçamento → cliente). **Guard no `movimentar_fato`**: saída/transferência com lote vencido/bloqueado → ValueError (lote vencido não é vendido). API `/api/estoque/lotes` POST estendido + `/lotes/<id>/status`, `/lotes/fefo`, `/lotes/recall`. OpenAPI 96→**99 paths**. Frontend: aba Lotes com coluna Status (badge vencido/bloqueado/ativo), Origem/Custo/Documento, botões **Bloquear/Reabrir** e **Recall** (clientes afetados); form com origem/custo/documento/observação. Testes `test_lote_rastreabilidade.py` (8: entrada ledger, FEFO por validade, exclui vencido/bloqueado, guard saída, controle por família, recall, API). Schema DEV **116**. **Suíte backend 354 esperada** (rodada por agente em background). Sem deploy.

- **2026-09-01 (Onda 2 EST completa):** EST-001 disponibilidade · EST-002/003 custo médio+estorno · EST-004 valorização · EST-005 parâmetros · EST-006 inventário cíclico · EST-007 endereçamento · EST-008 lote/validade. Próximo: **Onda 3 — ABC/necessidade (COM-001..006)**.

- **2026-09-01 (Onda 2 — EST-007 endereçamento):** migração **0115** cria `endereco_posicao` (depósito + código rua-módulo-posição-nível, UNIQUE deposito+codigo), `endereco_estoque` (saldo por posição/produto, `primaria`), `endereco_movimento` (log entrada/saída/movimentação). Service `services/endereco.py`: `listar_posicoes` (só ativas, busca por código), `criar_posicao` (rejeita duplicado), `excluir_posicao` (bloqueia se ocupada), `estoque_na_posicao`, `posicao_primaria` (produto tem posição primária por depósito), `movimentar` (origem→destino com validação de saldo; `de=None`=entrada, `para=None`=saída; loga tudo; 1ª colocação vira primária), `ultimos_movimentos`. API `/api/estoque/enderecos` GET/POST, `<id>` DELETE, `<id>/estoque`, `/movimentar`, `/primaria/<produto_id>`, `/movimentos`. OpenAPI 90→**96 paths**. Frontend: aba **Endereços** no Estoque (`estoque/enderecos.tsx`: criar posição, movimentar, saldo por posição). Testes `test_enderecamento.py` (7). **Suíte backend 346 passed** + frontend 34/typecheck/build + `git diff --check` verdes. Schema DEV **115**. Sem deploy.

- **2026-09-01 (fix — eliminação do `lastval()`):** o shim `catalog_server/pgsql.py` usava `SELECT lastval()` para popular `cursor.lastrowid` após INSERTs sem RETURNING, gerando erro no log do PG (`lastval is not yet defined in this session`) quando a sessão não avançava sequence (tabela sem serial / INSERT com id explícito). **Solução**: o shim agora executa o INSERT com `RETURNING id` **somente se a tabela tiver a coluna `id`** (checagem via `information_schema.columns` com cache `_HAS_ID_CACHE`, parsing do nome da tabela do INSERT) — tabelas sem `id` (ex.: `usuario_perfis`) fazem INSERT puro, `lastrowid=None`, **sem erro no log**. `last_insert_rowid()`→`lastval()` removido do `translate_sql`; `_set_lastrowid` removido. **16 helpers de teste** com `SELECT lastval()` convertidos para `INSERT ... RETURNING id` (testes de Onda 1/2 + compras/lancamentos/preços). Verificado: `lastval` = 0 ocorrências; teste de tabela sem id não loga erro. Backend reiniciado, health 200, sem erros no log. Suíte 346 passed (rodada por agente em background). Próximo: EST-008 (lote/série/validade, DECISAO-011).

- **2026-08-31 (Onda 2 — EST-006 inventário cíclico):** migração **0114** cria `inventario_ciclo` (depósito, nome, status planejado/em_andamento/aprovado/ajustado/cancelado, criado_por, fechado_em) e `inventario_contagem` (ciclo_id, produto_id, saldo_esperado, quantidade_contada, executor, contada_em, status pendente/conferido/divergente/ok/ajustado, aprovado_por/em, observacao; UNIQUE ciclo+produto). Service `services/inventario_ciclo.py`: `criar_ciclo` (gera lista de contagem = produtos com saldo ≠ 0 no depósito), `listar_ciclos` (com contagem pendentes/conferidas/divergentes), `detalhe_ciclo` (com diferença), `registrar_contagem` (conferido/divergente; bloqueia em ciclo ajustado/cancelado), `aprovar_ciclo` (**ajuste só após aprovação**; bloqueia com pendentes; aplica fato `inventario` via `lancar_inventario` idempotente por contagem; status ok/ajustado; ciclo → ajustado), `cancelar_ciclo`. API `/api/estoque/inventario/ciclos` POST/GET, `<id>` GET, `<id>/contagens`, `<id>/aprovar`, `<id>/cancelar` (prefixo `/api/estoque` → RBAC estoque). OpenAPI 85→**90 paths**. Frontend: `estoque/inventario-ciclo.tsx` (lista, criar, detalhe com contagem/observação/diferença, aprovar/cancelar) acessível pela aba Inventário via botão **"Ciclos de contagem"** (toggle simples×ciclos). Testes `test_inventario_ciclo.py` (7: cria lista, contagem status/diferença, aprovar ajusta saldo, idempotente, pendente bloqueia, cancelar, API fluxo). **Suíte backend 339 passed** + frontend 34/typecheck/build + `git diff --check` verdes. Migração **aplicada no DEV (schema 114)** + backend reiniciado. Sem deploy. Próximo: EST-007 (endereçamento — posições de armazenagem rua/módulo/posição/nível).

- **2026-08-31 (Onda 2 — EST-005 parâmetros de planejamento):** migração **0113** cria `estoque_parametro` por produto/depósito (política manual/calculada; mínimo/máximo; ponto de pedido; estoque de segurança; lead time; lote min/max/múltiplo; calendário; fonte do valor manual/abc/lead_time_real/custom; motivo; versão; criado_por) com índice único parcial `(produto_id, deposito_id) WHERE ativo` (versões anteriores desativadas → **histórico auditável**). Service `services/estoque_parametro.py`: `listar/salvar/excluir/obter_efetivo` — `obter_efetivo` faz **fallback para os limites legados** `estoque_saldo.estoque_minimo/maximo` (minimo_origem=legado/parametro). API `/api/estoque/parametros` GET/POST/DELETE + `/parametros/efetivo`. OpenAPI 83→**85 paths**. Frontend: aba **Estoque** no cadastro de produto (`produtos/parametros-estoque.tsx`) com grid + formulário por depósito; client `EstoqueParametro`. Testes `test_estoque_parametro.py` (8: upsert versiona, fallback legado, validações, excluir, API fluxo/inválido/RBAC). **Suíte backend 332 passed** + frontend 34/typecheck/build + `git diff --check` verdes. Migração **aplicada no DEV (schema 113)** + backend reiniciado. Sem deploy. Próximo: EST-006 (inventário cíclico com aprovação) e EST-007 (endereçamento).

- **2026-08-31 (Onda 2 — EST-004 valorização):** `estoque_repo.valorizar(deposito_id, data_corte, produto_id)` — quantidade × custo médio por depósito. Sem `data_corte` usa saldo/custo médio vigentes; com `data_corte` reprojeta saldo (último fato ≤ data via `saldo_posterior`) e custo médio (`custo_medio_anterior` do 1º fato após a data) a partir do ledger, **nunca editando fato**. API `GET /api/estoque/valorizacao` (deposito_id obrigatório). OpenAPI 82→**83 paths**. Frontend: card **Valorização do estoque** no Estoque/Saldo + client `valorizacaoEstoque`. Testes `test_estoque_valorizacao.py` (7: vigente, por produto, data de corte, vazio, API, sem depósito 400, RBAC 403). **Suíte backend 324 passed** + frontend 34/typecheck/build + `git diff --check` verdes. Backend reiniciado (rota ativa). Sem deploy. Próximo: EST-005 (parâmetros de planejamento) e EST-006 (inventário cíclico).

- **2026-08-31 (Onda 2 — EST-002/003 custo médio + estorno):** migração **0112**: `estoque_movimento` + `custo_unitario`, `custo_medio_anterior` e `estorno_de` (cadeia de estorno + índice); `estoque_saldo` + `custo_medio` (média ponderada por depósito, DECISAO-002). `movimentar_fato` agora: **entrada/inventário/ajuste definem o custo** (param `custo_unitario` ou fallback `produtos_cadastro.custo_unitario`) e recalculam a média `(saldo·média + q·custo)/(saldo+q)`; **saída/transferência usam o custo do momento** (média não muda); reserva/liberação não alteram custo. `estornar()` (EST-002): cria movimento reverso com `estorno_de` (nunca edita fato), **LIFO** (bloqueia se não for o último), restaura saldo (`saldo_anterior`) e custo médio (`custo_medio_anterior`), idempotente. `custo_medio()` helper + grid de saldo expõe `custo_medio`. Movimento grava `origem_tipo/origem_id/documento`. Frontend: coluna **Custo médio** no Estoque + tipo `SaldoItem`. Testes `test_estoque_custo.py` (10: média ponderada, custo do momento, fallback, estorno reverte, idempotência, já-estornado, LIFO, origem, grid). **Suíte backend 317 passed** + frontend 34/typecheck/build + `git diff --check` verdes. Migração **aplicada no DEV (schema 112)** + backend reiniciado. Sem deploy. Próximo: EST-004 (valorização por data de corte) e EST-005 (parâmetros de planejamento).

- **2026-08-31 (refactor — `variante_id` → `produto_id`):** renomeação completa do resquício pós v2.26.0 (não existe mais a tabela `variantes`; `variante_id` apontava para `produtos_cadastro.id`). Confirmado via `information_schema`: **nenhuma coluna de banco** chama `variante_id` (só `n_variantes`, contador válido). Refactor de código apenas (sem migração). **43 arquivos** (backend 28 + testes + frontend 5): 262 ocorrências backend + 26 frontend → **0 restantes** (grep). Contrato de API interno coordenado (path `<int:produto_id>`, query `?produto_id=`, JSON `produto_id`). **Casos especiais tratados**: `sku_service.gerar/reservar` tinham 2 params colapsados (produto/variante → único `produto_id`, formato do SKU `base-{id}-{id}` preservado; 5 chamadores atualizados); colisões de param/chave onde `variante_id` e `produto_id` coexistiam (`estoque.saldo`, `api_estoque.consultar_saldo`, tipo `PerfilFiscalEfetivo`, `fiscal_perfil.obter_efetivo` chave duplicada, `api_orcamentos` get redundante); teste `test_sprint1_modelo_dominio` atualizado para a nova assinatura. **Suíte backend 307 passed** + frontend 34/typecheck/build + `git diff --check` + backend reiniciado (rotas novas ativas, 401 sem token). Sem deploy.

- **2026-08-31 (Onda 2 — EST-001 disponibilidade):** migração **0111** adiciona `estoque_saldo.bloqueado/separacao/transito` (default 0). Fórmula única: **disponível = físico − reservado − bloqueado − separação** (trânsito é saldo próprio). `repositories/loja.py`: `saldo_variante`/`saldo_disponivel`/`disponibilidade` com o balanço completo; `estoque_repo.saldo` (grid) devolve `quantidade`/`reserva` (**aliases compatíveis**) + `fisico`/`reservado`/`bloqueado`/`separacao`/`transito`/`disponivel`; `situacao` (ruptura) agora considera o disponível. API `GET /api/estoque/disponibilidade/<produto_id>`. OpenAPI 81→**82 paths**. Frontend: grid do Estoque com colunas **Físico/Reservado/Disponível/Situação** + tipo `SaldoItem` ampliado. Testes `test_estoque_disponibilidade.py` (7) + **correção de testes frágeis** `test_api_movimentos_fato`/`test_api_inventario` (token do usuário 1 sem `system_db` herdava estado de teste anterior → agora dependem de `system_db`, banco fresco + bootstrap do admin). **Suíte backend 307 passed** + frontend 34/typecheck/build + `git diff --check` verdes. Migração **aplicada no DEV (schema 111)** + backend reiniciado. Sem deploy.

- **2026-08-31 (Onda 1 — MDM-007 regras de preço):** migração **0110** `preco_regra` (prioridade, canal, cliente_id, segmento, quantidade_min, preco/desconto_pct, margem_minima_pct, vigência, motivo, versão; CHECKs preço≥0/desconto 0-100/preço-ou-desconto; **Expand**). Service `preco_regra.py` (`listar/salvar/excluir`, `resolver` com prioridade menor vence + contexto casa + vigência). **`pricing_engine.preco_efetivo` estendido**: regra → tabela do canal → motor → base, devolvendo `origem="regra"`, `regra_id`, `explicacao` e flag `abaixo_da_margem_minima` (margem real × `margem_minima_pct`, DECISAO-008 via alçada). API: `GET/POST /api/precos/regras/<produto_id>`, `DELETE .../regras/<id>`; `preco_efetivo` aceita `cliente_id/segmento/quantidade`. OpenAPI 79→**81 paths**. Frontend: aba **Regras de preço** no cadastro de produto (`produtos/regras-preco.tsx`). Testes `test_preco_regra.py` (10: CRUD, prioridade/contexto, cliente/segmento/quantidade, efetivo com/sem regra, margem mínima, validações, API fluxo/inválida, RBAC 403; corrigido `dict.get(type=)` → conversão manual). **Suíte backend 300 passed** + frontend 34/typecheck/build + `git diff --check` verdes. Migração **aplicada no DEV (schema 110)** + backend reiniciado (rotas ativas). Sem deploy. Onda 1 completa (MDM-001..007; MDM-001 = decisão monoempresa registrada).

- **2026-08-31 (Onda 1 — MDM-005 relações entre produtos):** migração **0109** `produto_relacao` (tipo `equivalente/substituto/acessorio/complementar/componente`, fator>0, prioridade, vigência, aprovado, motivo, ativo, versao; CHECK tipo + fator + produtos diferentes; índice único parcial ativo `(produto_id,relacionado_id,tipo)`; **Expand**). Service `produto_relacao.py` (`listar` com filtro por tipo + vigência, `relacionados` nos dois sentidos com nome/SKU do outro produto, `salvar` com versionamento/upsert, `excluir`). API: `GET/POST /api/produtos-cadastro/<id>/relacoes`, `GET .../relacoes/relacionados`, `DELETE .../relacoes/<id>`. OpenAPI 76→**79 paths**. Frontend: aba **Relações** no cadastro de produto (`produtos/relacoes.tsx`). Testes `test_produto_relacao.py` (9: CRUD, versionamento, dupla direção, kit componentes, validações, API fluxo/inválida, RBAC 403; corrigido bug de coluna ambígua no JOIN). **Suíte backend 290 passed** + frontend 34/typecheck/build + `git diff --check` verdes. Migração **aplicada no DEV (schema 109)**. Sem deploy. Próximo: MDM-007 (preço/margem) e MDM-001 (modelo multifilial).

- **2026-08-31 (Onda 1 — MDM-004 atributos técnicos):** migração **0108** promove 11 colunas relacionais em `produtos_cadastro` (bitola, tensao, potencia, comprimento, diametro, rosca, material, cor, norma, validade_dias, garantia_dias) + **backfill idempotente** do JSONB `atributos` (chaves por nome normalizado unaccent+lower) + índices btree (bitola/cor/material/rosca). **Bug corrigido:** placeholders `?` no backfill eram inválidos no psycopg cru do runner de migrações → trocado para `%s` (migração rodada em banco vazio→head nos testes). Backfill aplicado no DEV: bitola 1286, rosca 1425, material 877, cor 510, potência 94, diâmetro 534 (tensão/comprimento/norma sem rótulos no catálogo atual). Repo `produtos.py`: create/update persistem as colunas via `dados` (helper `_to_int`). Frontend: campos **Atributos técnicos do ramo** na aba Dados do editor + tipos `ProdutoCadastro`/`Payload`. Testes `test_atributos_tecnicos.py` (5: persiste, atualiza, default vazio, colunas e índices existem). **Suíte backend 281 passed** + frontend 34/typecheck/build + `git diff --check` verdes. Migração **aplicada no DEV (schema 108)**. Sem deploy. Próximo: MDM-005 (equivalentes/substitutos/kits).

- **2026-08-31 (DECISAO-001..012 fechadas):** todas as decisões bloqueantes foram **decididas** e registradas em `docs/erp/decisoes/DECISAO-ERPs.md` (tabela de registro + seções por decisão). **DECISAO-003 (diretriz do usuário):** **Simples Nacional** (sistema preparado para **Lucro Real**) + API **FOCUS** + **Certificado A1**. Demais seguem a recomendação inicial: 001 monoempresa (modelo preparado), 002 custo médio por depósito (contador confirma na homologação), 004 TEF manual, 005 troca 30 dias/garantia por fornecedor, 006 comissão % venda líquida, 007 segurança por ABC, 008 reusar alçada de desconto, 009 marketplaces manual, 010 LGPD mascarar PII + consentimento, 011 lote por família, 012 conciliação manual assistida. `EXECUCAO_PLANO_MESTRE_ERP.md` atualizado (seção B + tabela de ondas: bloqueios resolvidos; FIS segue com gate de homologação). **Desbloqueia** MDM-001, EST-003/004/005/008, COM-004/005, MDM-007, VEN-007, POS-001..005, FIS-001..006, INT/ADM. Sem deploy.

- **2026-08-31 (Onda 1 — MDM-006 workflow + importação):** migração **0107** `produtos_cadastro.status_cadastro` (rascunho/em_revisao/publicado/bloqueado; default 'publicado' preserva existentes) + tabela `cadastro_importacao` (hash UNIQUE de idempotência, contadores, resumo, criado_por). Service `cadastro_importacao.py`: `set_status_cadastro` com transições controladas (publicar = ativo 1; demais = ativo 0), `preview` por linha (nome obrigatório, EAN 8/12/13/14, preço≥0), `importar` idempotente (hash → duplicado retorna resultado anterior), dedup por SKU/EAN/nome+marca, cria novos como **rascunho** (revisão antes de publicar) e registra auditoria do lote. API: `POST /api/produtos/importar/preview`, `POST /api/produtos/importar` (limite 1000 linhas), `PATCH /api/produtos-cadastro/<id>/status`. OpenAPI 73→**76 paths**. Frontend: botão **Importar lote** (`produtos/modal-importar-lote.tsx` com prévia/resultado), **StatusCadastro** no cabeçalho do editor, client `status_cadastro`+`importarProdutos`. Testes `test_cadastro_importacao.py` (8: preview, cria rascunho inativo, idempotência por hash, dedup por sku, transições+ativo, transição inválida, API fluxo, RBAC 403). **Suíte backend 276 passed** + frontend 34/typecheck/build + `git diff --check` verdes. Migração **aplicada no DEV (schema 107)**. Sem deploy.

- **2026-08-31 (Onda 1 — MDM-003 identificadores):** migração **0106** `produto_identificador` (tipo `ean/gtin/codigo_interno/fabricante/fornecedor/embalagem`, valor normalizado, embalagem opcional, origem, ativo; CHECK de tipo + índice único parcial ativo `(produto_id,tipo,valor)`; **Expand**). Service `catalog_server/services/produto_identificador.py` (`normalizar_valor` com validação GTIN 8/12/13/14, `listar/salvar/excluir` com upsert idempotente, `buscar` por código exato — identificador ativo/EAN/SKU — antes da busca textual). API: `GET/POST /api/produtos-cadastro/<id>/identificadores`, `DELETE .../identificadores/<id>`, `GET /api/produtos/por-codigo?q=` (RBAC produtos/catalogo). OpenAPI 70→**73 paths**. Frontend: aba **Códigos** no cadastro de produto (`produtos/identificadores.tsx`) + client `ProdutoIdentificador`. Testes `test_produto_identificador.py` (10: CRUD, GTIN inválido, dedupe, exclusão, busca por identificador/SKU/EAN, API fluxo/inválida, RBAC 403; corrigido bug do `termo_digits or termo` na busca). **Suíte backend 268 passed** + frontend 34/typecheck/build + `git diff --check` verdes. Migração **aplicada no DEV (schema 106)**. Sem deploy. Próximo: MDM-006 (workflow de cadastro + importação idempotente).

- **2026-08-31 (Onda 1 — MDM-002 conversões de unidade):** migração **0105** `unidade_conversao` (tabela versionada produto×origem→destino com fator>0, unidade_base, ativo, versao, criado_por; índice único parcial ativo; **Expand** — preserva `unidade_venda`/`fator_conversao`). Service `catalog_server/services/unidade_conversao.py` (`listar/obter/salvar/excluir` com versionamento de auditoria, `unidade_base`, `converter` com fallback para o legado e resolução direta/inversa/em cadeia). API em `api_produtos.py`: `GET/POST /api/produtos-cadastro/<id>/conversoes`, `DELETE .../conversoes/<origem>`, `GET .../conversao?qtd&de&para` (RBAC → recurso `produtos` via prefixo; autoria Bearer em `criado_por`). OpenAPI 66→**70 paths**. Frontend: aba **Conversões** no cadastro de produto (`produtos/conversoes.tsx`: lista, salvar, remover e testar conversão) + client `ConversaoUnidade`. Testes `test_unidade_conversao.py` (10: CRUD, versionamento sem duplicar, exclusão, conversão direta/inversa/cadeia, fallback, validações, API fluxo, API inválida, RBAC 403). **Suíte backend 258 passed** + frontend 34 testes/typecheck/build + `git diff --check` verdes. Migração **aplicada no DEV (schema 105)**. Sem deploy. Próximo: MDM-003 (produto_identificador) e MDM-006 (workflow de cadastro/importação). 

- **2026-08-31 (Onda 0 — Governança do plano mestre):** criado `EXECUCAO_PLANO_MESTRE_ERP.md` (plano de execução do `PLANO_MESTRE_IMPLEMENTACAO_ERP.md`, ordem §21; `PLANO_EXECUCAO_8_PENDENCIAS.md` permanece pendente/intocado). **GOV-001** `docs/erp/processos-operacionais.md` (matriz de 12 processos: ator/estados/efeitos EST·FIN·FIS·CONT/permissão/exceções/responsável). **GOV-002** `docs/erp/contracts/dicionario-dados.md` (campos comuns, identificadores, unidades, dinheiro/percentual, erros `{error,code,details,correlation_id}`, paginação, regras para novas APIs). **GOV-003** `docs/erp/contracts/maquinas-estado.md` (8 máquinas: venda, compra, recebimento, estoque-fatos, fiscal, financeiro, RMA, garantia — proíbe `PATCH` livre e define idempotência/estorno). **GOV-004** 5 flags novas registradas em `catalog_server/flags.py::REGISTRADAS` (`MOTOR_COMPRAS`, `ABC_HISTORICA`, `CUSTO_HISTORICO`, `ENTRADA_FISCAL`, `NOVO_RECEBIMENTO` — o painel lista automaticamente via `flags.listar()`). **DECISAO-001..012** documentadas em `docs/erp/decisoes/DECISAO-ERPs.md` (mono/multi, custo, fiscal, TEF, POS, comissão, níveis, margem, marketplaces, LGPD, lote, bancos). Validação: `py_compile` flags.py, mojibake 0 nos novos docs, `git diff --check` limpo. Sem deploy. Próximo: Onda 1 — MDM-002/003/006 (depende de fechar DECISAO-001/002 para MDM-001/EST-003).

- **2026-08-31 (plano mestre de implementação):** criado `PLANO_MESTRE_IMPLEMENTACAO_ERP.md`, derivado do levantamento de lacunas. O plano detalha 15 etapas numeradas (Sprint 0 até Sprint 14) e tarefas identificadas por domínio (`GOV`, `MDM`, `EST`, `COM`, `REC`, `VEN`, `POS`, `FIS`, `BI`, `UX`, `ARC`, `INT`, `ADM`, `PIL`), com escopo, abordagem de banco/backend/API/frontend, contratos, RBAC, idempotência, concorrência, testes, critérios de aceite, dependências e gates de rollout. Inclui catálogo de relatórios administrativo/compras/estoque/vendas/fiscal/financeiro e checklist prescritivo para agentes. Nenhum código de aplicação, migration, deploy, restart ou operação em staging/produção foi executado.

- **2026-08-31 (levantamento de lacunas do ERP):** realizada auditoria de escopo em todo o projeto para avaliar a prontidão de uma loja de materiais elétricos, hidráulicos, ferragens e ferramentas. O relatório completo foi criado em `RELATORIO_LACUNAS_ERP.md`. O diagnóstico preserva como existentes catálogo, pré-venda, compras/cotações, estoque, financeiro, fiscal, RBAC, outbox/webhooks e base de UI; classifica como pendências prioritárias a homologação fiscal real, reconciliação transacional ponta a ponta, motor de necessidade de compra, ABC histórica/XYZ, custo/valorização, recebimento profissional, inventário cíclico, relatórios gerenciais, PDV/TEF, devoluções com efeitos fiscais/financeiros, E2E e operação de produção. Nenhum código de aplicação, deploy, restart ou migração não-dev foi executado nesta auditoria.

- **2026-08-29**: auditoria técnica/UX de todo o workspace concluída em modo somente leitura; nenhum deploy, restart, rebuild de ambiente ou migração foi disparado.
  - Backend: identificados riscos críticos de atomicidade no fechamento/reabertura de vendas, concorrência no saldo de estoque/caixa/contas, baixa automática de pagamentos, exposição de credenciais e SSRF/uploads sem limites.
  - Fiscal: emissão ainda deve ser tratada como não pronta para produção até XSD, assinatura, idempotência, autenticação de webhooks e homologação SEFAZ/Focus; há código estrutural com `aamm=0000` e campos tributários simplificados.
  - Frontend: build e typecheck passam, porém há módulos monolíticos, cliente HTTP sem cache/schema runtime, mistura React + DOM imperativo, modal sem ciclo completo de acessibilidade e cobertura funcional baixa.
  - Infra/testes: `python -m compileall` passou; `npm run typecheck`, `npm run build` e 13 testes Vitest passaram. Sem `TEST_PG_URL`, a suíte backend encerra por segurança; o `testpaths` do `pyproject.toml` evita a colisão com o subprojeto `cotacoes-ia-importer`.
  - Prioridade sugerida: (1) transação única/UoW com locks e idempotência para vendas/estoque/financeiro; (2) bloquear segredos/defaults e validar webhooks; (3) endurecer fetch/upload; (4) completar pipeline fiscal; (5) modularizar frontend e ampliar testes E2E.
- **2026-08-30**: pacote de hardening e consistência implementado localmente, sem deploy, restart ou migração aplicada em ambiente não-dev.
  - Vendas, reabertura, estoque, caixa, contas a receber e contabilidade passaram a compartilhar transação quando necessário, com `FOR UPDATE`, locks/advisory locks, validação de saldo e idempotência nas movimentações.
  - Segredos de provedores de pagamento não são mais expostos na API; credenciais vazias preservam o valor existente; ambiente de cobrança passou a ser persistido pela migração versionada `0097_cobranca_ambiente`.
  - RBAC deixou de aceitar bypass legado por token e usuários inativos são rejeitados; bootstrap administrativo não usa mais senha fixa em desenvolvimento e é bloqueado em produção sem configuração explícita.
  - Webhooks de pagamento/fiscal exigem segredo configurado, uploads têm limite global/extensões permitidas e downloads de imagens/HTML rejeitam SSRF, redirecionamentos inseguros e respostas acima do limite.
  - Modal principal recebeu foco inicial, trap de Tab, Escape, `aria-modal`/label e restauração de foco; contrato TypeScript de credenciais foi ajustado para segredos opcionais.
  - Validação: backend completo `215 passed`, pagamentos `7 passed`, frontend `13 passed`, `npm run typecheck`, `npm run build` e `py_compile` passaram. A configuração de produção agora exige `APP_VERSION`, credenciais PostgreSQL, `CATALOG_SECRET` e `CATALOG_ENV=production`.
  - Pendências intencionais: assinatura nativa por provedor nos webhooks, homologação real Focus/SEFAZ e validação fiscal XSD/assinatura, outbox/jobs assíncronos, rate limit distribuído e modularização ampla das telas React. Não ligar `FISCAL_ENGINE_V2` nem publicar sem confirmação explícita.
- **2026-08-30 (continuação)**: rate limit de login implementado com estado persistido no PostgreSQL e migração versionada `0098_login_rate_limit`; limita por IP e conta, retorna `429`/`Retry-After` e remove a janela após login válido. A suíte completa banco vazio → head terminou com `215 passed`.
  - A integração do site institucional permanece pendente: o projeto existe em `C:\Users\jpsantos\Documents\Projetos\CASA_LM\site` (repo separado), mas seus consumidores ainda usam fallback SSR de catálogo demonstrativo e precisam ser validados contra a API real.
  - TLS/Let's Encrypt, sincronização da VM, homologação Focus/SEFAZ, ativação de `FISCAL_ENGINE_V2`, limpeza de imagens e renomeação da pasta continuam bloqueados por operação, credenciais, dados reais ou autorização de publicação.
  - Outbox/Celery permanece pendente: o projeto não possui worker/dependência configurado e a implementação exige definir quais integrações serão assíncronas e seu contrato de reprocessamento antes de alterar o fluxo fiscal/financeiro.
  - Plano sequencial das oito pendências registrado em `PLANO_EXECUCAO_8_PENDENCIAS.md`, com critérios de aceite, pré-requisitos e gates de rollback.
- **2026-08-30 (Fase 0 do plano)**: migrações `0097`+`0098` **aplicadas no DEV** (schema 98); suíte backend **215 passed** + typecheck/build frontend OK; `deploy.yml` corrigido para injetar `CATALOG_SECRET`, `POSTGRES_USER`, `POSTGRES_PASSWORD` (secrets — sem isso o próximo deploy falharia). Regressões do hardening validadas ao vivo: Vendedor lê `/api/sistema/status` (200) e é negado em cadastrar produto (403); login com rate limit ok; API pública ok. Pendente de autorização: deploy de staging (P1), sincronizar VM (P7), integração do site `../CASA_LM/site` (P4).
- **2026-08-30 (P1 + P7 do plano)**: **Secrets configurados no GitHub** (`CATALOG_SECRET` aleatório, `POSTGRES_USER`/`POSTGRES_PASSWORD=catalog`). **Staging deploy validado** (run 33293628345, sucesso — migrações 0097/0098 em banco vazio→head, smoke, rate limit). **VM 10.189.14.9 sincronizada** (git pull até `4c77c8e`, stack rebuild, `versioning apply` → schema 98; health + pronto OK). Manifesto v2.34.0 (hardening) criado e pushado.
- **2026-08-30 (P8 dry-run)**: relatório de saneamento de imagens gerado e commitado (`reports/*`): 3.188 produtos sem imagem (3.181 ativos), 0 linhas quebradas, **5.648 arquivos órfãos (435 MB)** e **2.742 pastas órfãs (1.060 MB)**. Nenhum sem-imagem tem arquivo órfão (precisa lote por fornecedor). **Tarefa deixada pendente pelo usuário** (aguarda aprovação para lote + remoção).
- **2026-08-30 (P2 — Webhooks e segredos)**: **validação de assinatura/token nativo por provedor** implementada (`catalog_server/payments/`): Mercado Pago (x-signature HMAC-SHA256 + **anti-replay** por `ts` na janela `WEBHOOK_TS_WINDOW_MS`, default 5min), Asaas (`asaas-access-token`), EfiPay (`?token=` ou `x-efi-webhook-token`), Sicoob/TecnoSpeed (genérico `X-Webhook-Secret`; Sicoob usa mTLS na infra). Migração **0099** (`payment_provider_config.webhook_secret`), aplicada no dev (schema 99). `processar_webhook` valida **antes** de processar (401 em inválido); idempotência por `webhook_id` mantida (repetido não baixa 2x). Frontend: campo "Segredo do Webhook" na tela Integrações + tipo TS. **224 testes** backend verdes (8 novos de assinatura) + typecheck OK. **Não deployado** (aguarda autorização).
- **2026-08-30 (P2 — staging TLS + webhook operacional)**: staging com **HTTPS :444** (cert de produção via volume `siscom_certbot-etc`, `NGINX_MODE=staging`, HTTP :8081 + HTTPS :444). NAT pública **6174 → 444** configurada (acessível). Job `webhook-test` no `maintenance.yml` (configura provedores + valida assinaturas no staging). **Asaas sandbox configurado no staging** (api_key + webhook_secret via API) para **pix e boleto** — endpoint validado: token real → **200**, sem/errado → **401**. Provider não configurado → **503** (não 502). UI de Integrações ganhou badges `credenciais ✓/webhook ✓` + placeholder "já configurado" (commit `ff2f342`). **Pendente**: reativar a fila do webhook no Asaas (após 15 falhas) e revalidar entrega real.
- **2026-08-30 (P2 — baixa por notificação validada)**: fluxo completo comprovado no staging — criar cliente com CPF → conta a receber → **emitir PIX (Asaas sandbox)** → **webhook simulado** (evento + token) → conta **`pago`/saldo 0**. Bug real corrigido: lançamento no caixa falhava (`'PgRow' object has no attribute 'get'` — `conta` não normalizada para dict) → fix `0173d44` + redeploy staging. **Deploy Staging ganhou modo `rapido`** (pula suítes CI; ~4 min vs ~20) e modo `completo`. **Acesso direto ao servidor**: SSH `root@10.189.14.8` (chave `id_rsa`), repo persistente em `/home/jpsantos/siscom/repo` — deploys diretos (~2 min, sem GitHub): `git pull && docker compose -p siscom-staging -f deployment/compose/docker-compose.staging.yml up -d --build <svc> && versioning apply`.
- **2026-08-30 (P2 — logs de webhook + rechecagem)**: **tabela `webhook_log`** (migração **0100**, schema 100) registra toda notificação (provider, evento, payment_id, status, http_status, assinatura_ok, ip, payload resumo, erro). Endpoints `GET /api/webhooks/logs`, `GET /api/webhooks/logs/{id}`, `POST /api/webhooks/rechecagem` (consulta o provedor das cobranças pendentes e baixa as pagas; filtros provider/limite). RBAC → `financeiro`. Tela **Webhooks** no menu Financeiro (lista com filtros, detalhe com payload, botão rechecagem). **228 testes** backend + typecheck/build verdes. Deployado no staging via **SSH direto**. Validado: webhook 401 → log `nao_autorizado`; logs listáveis; rechecagem 200.
- **2026-08-30 (P5 — Outbox/assíncrono: fundação RQ + Redis)**: serviço **Redis** (redis:7) + **worker RQ** (processa fila `default`) + **scheduler** (enfileira a **rechecagem periódica** a cada `RECHECAGEM_INTERVAL_MIN`, default 15) nos 3 composes (dev/staging/prod). Deps `redis`+`rq` (pyproject + Dockerfile). Módulo `catalog_server/jobs/` (`tasks.py` rechecagem, `worker.py`, `scheduler.py`). Resultado da rechecagem periódica registrado em `webhook_log` (evento `rechecagem_agendada`). Lições: RQ 2.12 usa `job_timeout` (não `timeout`) e o broker NÃO pode ter `decode_responses=True`. **Validado no staging**: scheduler enfileira, worker processa (`Job OK` ~0,8s), log atualizado. Outbox transacional (webhooks/imagens/integrações) = próximo passo do P5.
- **2026-08-30 (P5 — Outbox transacional)**: tabela **`outbox`** (migração **0101**, schema 101): `topico/payload/status/tentativas/proxima_tentativa/ultimo_erro/idempotencia_key`. `services/outbox.py` (`enfileirar` com idempotência, `prontas`, `marcar_ok/erro` com **backoff exponencial** (60s·2ⁿ) e **dead-letter** após `MAX_TENTATIVAS=5`). `jobs/tasks.py`: `rodar_outbox` (handler registry, retry/backoff/dead-letter) + consumidor `webhook.rechecagem` (recheca uma conta específica). **Webhook 503** (provider não configurado) agora **enfileira a rechecagem da conta** (idempotente por `webhook:provider:payment_id`) — quando o provedor for configurado, o worker baixa a conta sem nova notificação. Scheduler enfileira `rodar_outbox` a cada `OUTBOX_INTERVAL_SEC` (default 60). Endpoints `GET /api/webhooks/outbox` e `POST /api/webhooks/outbox/rodar` (RBAC financeiro). **234 testes** verdes. Validado no staging: webhook efipay (sem config) → 503 → outbox `webhook.rechecagem` → worker processou → `ok`.
- **2026-08-30 (P6 — Frontend: contrato de erro + testes)**: **`ApiError` tipado** no client HTTP (`status/code/details`) + helper `mensagemErro` (formata para toast) — usado em `request` e `enviarArquivo`. **Testes do client** (mock fetch: 200 JSON, 4xx com error/code, 5xx, falha de rede → 'Servidor indisponível') e **Table/Badge** (EmptyRow, data-label, cards mobile, tons de badge). **27 testes frontend** (era 13) + typecheck/build verdes. Deployado no staging via SSH. Pendente (P6): modularizar telas grandes (produtos 2.4k linhas), query client/cache, virtualização, E2E de fluxos (PDV/estoque/financeiro).
- **2026-08-30 (P6 — Modularização de tela)**: **`financeiro.tsx` modularizado** (1279 → **634 linhas**): extraídos para `src/pages/financeiro/` os componentes autocontidos `AnexoButton`, `ModalLancamento` (parcelamento/recorrência), `Condicoes`, `Centros` e `Adiantamentos` (extração verbatim, sem mudança de comportamento). Limpeza de imports (`useRef`, `Textarea`, tipos não usados). typecheck/build/27 testes verdes. Deployado no staging via SSH. Próximas: `produtos.tsx` (2.4k), `compras.tsx` (1.4k), `fiscal.tsx` (1.3k), `pre-venda.tsx` (1.3k), `precos.tsx` (1.3k).
- **2026-08-30 (P6 — Modularização de tela: produtos)**: **`produtos.tsx` modularizado** (2406 → **1904 linhas**): extraídos para `src/pages/produtos/` os autocontidos `Imagens`, `PerfilFiscalPanel`, `ModalImportarUrl`+`PreviewRow`, `ModalImportarCatalogo` e `ModalEtiquetas` (verbatim; sem mudança de comportamento; `buildAtributosState`/`seedFornecedorRows` permanecem no pai por serem usados por ele). Limpeza de imports órfãos. typecheck/build/27 testes verdes. Deployado no staging via SSH. Próximas: `compras.tsx` (1.4k), `fiscal.tsx` (1.3k), `pre-venda.tsx` (1.3k), `precos.tsx` (1.3k).
- **2026-08-30 (P6 — telas grandes concluídas)**: modularizadas **`compras.tsx` (1437→862)**, **`fiscal.tsx` (1341→77)**, **`pre-venda.tsx` (1308→928)** e **`precos.tsx` (1286→68)** — total **36 módulos** extraídos. **Correção de encoding**: `financeiro.tsx`/`produtos.tsx` (e o compras na 1ª tentativa) tiveram acentos corrompidos por `Set-Content` ANSI — restaurados via `git checkout` dos commits-limpos e extração refeita com `-Encoding UTF8` (auditoria por bytes de todos os módulos: 100% UTF-8). Sem mudança de comportamento (extração verbatim). typecheck/build/27 testes verdes; deploy no staging pendente.
- **2026-08-30 (P6 — telas médias concluídas)**: modularizadas **`orcamentos.tsx` (545→278)**, **`caixa.tsx` (898→274; `DataBox` promovido para `ui/data-box.tsx` — dedup com pre-venda)**, **`estoque.tsx` (823→58)** e **`configuracoes.tsx` (570→33)** — +**21 módulos** (total 57). **Correção de mojibake pré-existente**: `pre-venda.tsx`, `orcamentos.tsx`, `configuracoes.tsx` e módulos estavam double-encoded (UTF-8→cp1252→UTF-8) — corrigido com des-mojibake por bytes (fixer cp1252 programático, inócuo em arquivos limpos; dry-run em todo o frontend = 0 arquivos com mojibake). typecheck/build/27 testes verdes.
- **2026-08-30 (P6 — P6 CONCLUÍDA, 29 telas)**: levantamento das 19 telas restantes + plano em 4 fases executado. **Fase 1** (verbatim top-level): `cotacoes` (867→296), `catalogo` (431→302), `posvenda` (423→33), `bancos` (350→33), `atualizacoes` (372→352), `historico` (202→72), `dashboard` (208→201). **Fase 2** (modal-forms autocontidos): `clientes` (864→147), `fornecedores` (487→151), `usuarios` (285→131), `perfis` (285→248). **Fase 3** (modais CRUD): `unidades` (146→112), `vendedores` (131→96), `plano_contas` (138→99), `solicitacoes` (119→85), `diagnostico_variacoes` (156→130). **Total: 93 módulos** em 26 pastas. **Fase 4 (mantidas, sem ganho viável)**: `categorias` (381 — árvore interativa de componente único), `webhooks` (215 — componente único sem modal), `recebimento` (118 — `ModalRecebimento` já exportado e reutilizado). Mojibake corrigido em todo o frontend (dry-run = 0). typecheck/build/27 testes verdes; deploy no staging via SSH pendente.
- **2026-08-30 (auditoria Codex das implementações recentes)**: revisão somente leitura do backend, frontend, composes, workflows e site institucional. Achados P1: baixa de conta e caixa não são uma transação recuperável em webhook/rechecagem/consulta; outbox não faz claim concorrente, SELECT→INSERT não é atômico e retry pode ser marcado como sucesso em JSON inválido/resultado com `erros`; healthcheck produtivo fixa usuário `catalog`; staging monta volume de certificado descrito como produção; home estática do site usa catálogo demonstrativo no SSR. Achados P2: emissão de cobrança sem lock de emissão; payload JSON não-objeto pode gerar 500; CORS público é wildcard. Validação limpa: backend isolado `234 passed`; frontend `27 passed`, typecheck/build OK; Astro build OK. Nenhum deploy, restart ou migração não-dev executado. Correções devem ser priorizadas antes de publicação.
- **2026-08-30 (correções da auditoria Codex)**: migração `0102_outbox_claim` criada e aplicada no DEV (schema 102), com claim `FOR UPDATE SKIP LOCKED`, lease para recuperação e upsert idempotente; baixa por webhook/rechecagem/consulta passou a confirmar conta e caixa na mesma transação; emissão usa lock da conta e chave idempotente nos gateways; payloads inválidos e falhas de handler acionam retry; comprovantes validam assinatura de arquivo e removem arquivo órfão em erro; CORS aceita apenas origens configuradas; healthcheck produtivo usa usuário/banco do ambiente; staging mantém HTTPS na porta `:444` com o certificado compartilhado em somente-leitura, sem alterar a porta 443 da produção; Error Boundary adicionado ao shell React; site Astro deixou o SSR sem dados demonstrativos, mantendo fallback somente no navegador. Validação: `236 passed` backend, `29 passed` frontend, typecheck/build Vite e build Astro sem catálogo demonstrativo na home. Nenhum deploy ou restart foi executado; publicação permanece aguardando confirmação explícita.
- **2026-08-30 (P6 residual + P8)**: client HTTP passou a usar cache de `GET` por 15s, invalidação em mutações/troca de usuário e validação runtime mínima para `/api/produtos` e `/api/produtos-cadastro`; frontend ficou com **29 testes** verdes. A auditoria dos relatórios P8 foi refeita sem apagar arquivos: os candidatos `images/cadastro/62455` e `62461` seguem pendentes de aprovação explícita e backup.
- **2026-08-30 (UX Compras/Orçamentos)**: fluxo de Compras recebeu hierarquia visual por etapa, contagem de itens/unidades, busca e seleção em lote de fornecedores, prevenção de fornecedor avulso duplicado, estados vazios orientativos e ações acessíveis. A comparação ganhou indicadores de cobertura/respostas, estratégia de compra explícita, pesos do recomendado mais claros e matriz com cabeçalho/primeira coluna fixos; geração de pedidos fica bloqueada sem preço utilizável. Cotações, respostas pendentes, pedidos gerados/recebidos e detalhe de Orçamentos receberam cards de situação, status e próxima ação. Validação: **29 testes frontend**, `npm run typecheck`, `npm run build` e `git diff --check` verdes. Backend fora do ar impediu validação visual autenticada/E2E; nenhum deploy ou restart foi executado.
- **2026-08-30 (Pré-venda — pesquisa e teclado)**: busca de produtos passou a invalidar respostas antigas em voo, evitando resultados obsoletos após digitação rápida. O campo agora é um combobox/listbox acessível com `aria-activedescendant`, status de seleção, Home/End e instruções de teclado; `Ctrl+K` retorna à pesquisa, Enter adiciona, Enter na quantidade retorna ao campo e as linhas do cupom aceitam foco, ↑/↓, Enter/Espaço e Delete. Atalhos F1/F2/F3/F5/F6/F7/F8 ficaram identificados nos próprios botões. Adicionados 4 testes unitários de `parseBusca` (total **33**). `npm test`, `npm run typecheck`, `npm run build` e `git diff --check` verdes; backend fora do ar impediu E2E autenticado. Nenhum deploy ou restart foi executado.
- **2026-08-30 (Pré-venda — fluxo Enter)**: quando o campo de descrição está vazio, Enter agora inicia o fluxo operacional Desconto → Condição de pagamento → Observação → botão Finalizar. A Observação deixou de ficar oculta no mobile, evitando foco em controle invisível; a seleção de produto por Enter continua preservada quando existem sugestões. Validação: **33 testes frontend**, `npm run typecheck`, `npm run build` e `git diff --check` verdes. Nenhum deploy ou restart foi executado.
- **2026-08-30 (Correção de foco no Modal)**: o `Modal` compartilhado deixou de refazer o foco inicial a cada renderização causada pela digitação. O callback de fechamento agora é atualizado via `ref`, enquanto o trap de Tab/Escape e a restauração do foco continuam ativos somente durante o ciclo de abertura. Adicionado teste de regressão que renderiza novamente o formulário após alterar o login e confirma a permanência do foco no campo. Validação: **34 testes frontend**, `npm run typecheck`, `npm run build` e `git diff --check` verdes. Nenhum deploy ou restart foi executado.
- **2026-08-30 (Cadastro rápido de cliente no PDV)**: corrigido o contrato de erro HTTP da API para respostas JSON e o cliente HTTP agora lê o corpo antes de limpar a sessão em `401`, exibindo mensagem clara de sessão expirada. O modal de cadastro rápido usa `mensagemErro`. Corrigido bug de sequência PostgreSQL: o cliente padrão inserido explicitamente com `id=1` agora sincroniza o `BIGSERIAL`, evitando `UniqueViolation` no primeiro cliente real. Adicionado teste de integração garantindo que um usuário vinculado a Administrador passa o RBAC e cadastra cliente. Validação: **237 testes backend**, **34 testes frontend**, `npm run typecheck`, `npm run build`, `py_compile` e `git diff --check` verdes. Nenhum deploy, restart ou migração foi executado.
- **2026-08-30 (auditoria completa do RBAC)**: revisão somente leitura do modelo, migrações 0075–0077, autenticação HMAC, gate central, endpoints de perfis/usuários, rotas sensíveis e frontend. Achados P1: atribuição de Administrador/overrides sem limite pela alçada do ator; emissão fiscal classificada como `orcamentos.cadastrar`; rejeição de desconto sem checagem de aprovador; formulário não consegue revogar corretamente perfis/overrides vazios; ausência de proteção contra remoção do último administrador. Achados P2: logout sem revogação de token, autoria dependente de sessão Flask em endpoints Bearer, ausência de auditoria RBAC, ação de impressão de orçamento classificada incorretamente e UI filtrando somente `visualizar`. A checagem existente cobre recurso em **366 rotas**, mas não a ação efetiva. Validação: **21 testes backend direcionados**, **34 testes frontend** e `typecheck` verdes. Nenhuma alteração comportamental, migração, restart ou deploy foi executado. Próximo passo: corrigir os P1 com testes de regressão e contrato de auditoria.
- **2026-08-31 (correções da auditoria RBAC)**: implementadas as migrações `0103` (auditoria RBAC + `token_version`) e `0104` (ação fiscal `emitir` para Operador). O backend agora limita atribuição de Administrador/overrides ao superusuário, impede remoção/desativação do último administrador com lock transacional, revoga tokens no logout/troca de senha, rejeita tokens legados sem versão, usa o sujeito Bearer para autoria, aplica segregação no rejeite de descontos, diferencia impressão e emissão fiscal, valida payloads antes de persistir alterações e expõe auditoria. O frontend passou a sinalizar/desabilitar ações críticas por permissão, mantendo o backend como autoridade. Validação: **248 testes backend**, **34 testes frontend**, `typecheck` e `build` verdes. Nenhum deploy, restart ou migração não-dev foi executado; staging aguarda autorização explícita.
- **2026-08-31 (padrão visual ERP cloud)**: refinado o design system compartilhado do frontend sem alterar contratos de API ou regras de negócio. Shell com navegação e estados ativos mais claros, superfícies/cores semânticas, foco acessível e redução de movimento; tabelas com cabeçalho/hover e cards mobile consistentes; campos com associação automática label-controle e estados disabled/invalid; modais e loading com acabamento/skeleton; login com composição visual responsiva. Validação: **34 testes frontend**, `typecheck` e `build` verdes. Nenhum deploy ou restart foi executado.
- **2026-08-31 (regra permanente de UI/UX)**: o `AGENTS.md` passou a exigir, em todo desenvolvimento frontend, o padrão de ERP cloud baseado em tabelas adaptativas, navegação por teclado, foco visível, ações por linha, validação contextual, estados de loading/erro/empty, associação label-controle, RBAC refletido na UI e validação desktop/mobile. Referências normativas: Salesforce Lightning Datatable e SLDS.
- **2026-09-01 (plano complementar de crédito e auditoria)**: criado `PLANO_CREDIARIO_MULTI_TENANCY_AUDITORIA_ERP.md` com backlog, blueprint, sprints, regras de negócio e matriz de testes para crediário, recebimento, pré-venda, compras/estoque, etiquetas/URLs, auditoria e validação integral do código. Por decisão do usuário, **multi-tenancy foi excluído do escopo**: a aplicação permanece monoempresa/monobanco nesta fase, sem `tenant_id`, database-per-tenant, schema-per-tenant ou RLS; qualquer evolução futura exigirá ADR e plano próprios. Nenhum código, migration, restart ou deploy foi executado nesta sessão.
- **2026-09-01 (execução do plano de crediário e auditoria)**: implementada a migração versionada `0143_crediario_aprovacao` (aprovacao, eventos auditáveis e reservas), serviço de decisão com lock pessimista, segregação Financeiro/Vendas/Caixa e endpoints de solicitação/aprovação/bloqueio/suspensão/histórico. Venda a prazo agora exige crediário aprovado, limite/prazo/vigência e política de atraso; `CONSUMIDOR` só aceita à vista; vendedor não recebe o próprio pedido e recebimento usa uma única transação com idempotência por `Idempotency-Key`. Corrigidos payloads JSON inválidos, condições estruturadas no PDV, cadastro comercial com limite zero, saldo de estoque na aba Estoque, proxy de etiquetas/URLs com porta e validação de IDs de etiqueta. Incluído painel de crediário em Clientes e contratos OpenAPI. Validação: **534 testes backend**, **39 testes frontend**, `npm run typecheck`, `npm run build`, JSON OpenAPI e `py_compile` verdes. Migração não foi aplicada em DEV/staging/produção; nenhum container foi reiniciado e nenhum deploy foi executado.
- **2026-09-02 (testes, logs e documentos)**: o harness PostgreSQL foi otimizado sem perder isolamento: mantém `TRUNCATE ... RESTART IDENTITY CASCADE` por teste, mas reutiliza o engine/pool da sessão e a lista de tabelas é descoberta uma vez. O access logger passou a ignorar somente as linhas bem-sucedidas dos probes `/api/health` e `/api/pronto`; falhas e demais rotas continuam visíveis. Corrigida a validação de CNPJ no frontend (pesos dos dois dígitos verificadores) e criada a mesma regra no backend, com normalização de `f/j`, valor sem máscara e erro `documento_invalido`. Regressões adicionadas para CPF/CNPJ e filtro de logs. Validação: **8 testes backend direcionados**, teste RBAC de mapeamento `1 passed`, **41 testes frontend**, `npm run typecheck`, `py_compile` e `git diff --check` verdes. A suíte completa não foi repetida nesta sessão por durar cerca de 24 minutos; nenhum deploy, restart ou migration foi executado.

- **2026-09-02 (nova auditoria de prontidão para grande varejo e rede de profissionais)**: realizada auditoria independente, somente leitura, confrontando o código atual com migrations, banco DEV, serviços, blueprints, telas, cliente de API, testes e documentação. Criado `RELATORIO_AUDITORIA_GRANDE_VAREJO_PARCEIROS.md`. Confirmados riscos ainda abertos: banco DEV no schema **142** sem as tabelas da migration `0143_crediario_aprovacao`; recebimento de compra ignora condição e cria contas a pagar simplificadas/duplicáveis em parcial; três vias indexa por produto e perde linhas repetidas; conciliação pode atualizar `contas_receber` ao conciliar `contas_pagar`; retry de pagamento pendente pode marcar venda como recebida e confirmação concorrente pode duplicar caixa; diversos endpoints críticos ainda aceitam ator (`usuario_id/operador_id/aprovador_id/vendedor_id`) no payload; ABC histórica não implementa consumo nem depósito e aplica classe global; reposição mistura depósitos, trânsito/demanda global e possui N+1; relatórios têm agrupamentos incorretos e DRE/estoque/compras incompletos; precificação sinaliza margem mínima, mas não protege a venda. Não foram encontrados domínio, migration, serviço, endpoint ou tela de parceiros profissionais, fidelidade, pontos/cashback, indicação ou bonificação. O relatório propõe ondas 0–7, blueprint de ledger/auditoria para parceiros e benefícios, rentabilidade por obra/consumo e gates de prontidão. `npm run typecheck` passou; testes backend não rodaram nesta auditoria por ausência de `TEST_PG_URL` no host e de `pytest` no container. Nenhum código de aplicação, migration, restart ou deploy foi executado.

- **2026-09-02 (execução do plano — ondas 0–2 e início das ondas 3–6)**: implementadas e aplicadas somente no DEV as migrações `0144_integridade_financeira`, `0145_destino_operacional`, `0146_programa_parceiros`, `0147_tres_vias_linhas_rbac_parceiros` e `0148_indicacao_orcamento` (schema DEV **148**, sem pendências). Pagamentos ganharam locks/idempotência de caixa; recebimento avançado e endpoint legado passaram a compartilhar postagem transacional, parcelas, condição do fornecedor e compatibilidade de contrato; três vias passou a identificar linha de pedido; conciliação distingue receber/pagar e usa actor Bearer; destinos de depósito foram propagados; ABC/reposição e relatórios foram corrigidos para depósito, CMV/receita líquida e demanda real. Criado domínio de parceiros com cadastro/status auditável, indicação, ledger append-only, política versionada, bônus pendente/aprovável e vínculo transacional indicação→orçamento→venda concluída; RBAC separa aprovação financeira. Criada tela `#/parceiros` com filtros, indicadores, cadastro, ativação, geração de indicação e extrato responsivo/acessível. Caixa concorrente foi endurecido e operador não aprova a própria sessão. Validação: rodada backend completa **544 passed e 2 falhas de compatibilidade**, corrigidas e cobertas por regressão direcionada **14 passed**; testes frontend **41 passed**, `npm run typecheck`, `npm run build` e `py_compile` verdes. Nenhum deploy, restart ou migration fora do DEV foi executado. Próximo: completar política/tela de fidelização e pagamento de bônus, integração de código de indicação no fluxo de pré-venda, rentabilidade por obra/consumo e BI/E2E de release.
- **2026-09-02 (continuação — pagamento de bônus)**: concluída a transição financeira auditável `pendente → aprovado → pago` em `parceiro_bonus`, com endpoint RBAC específico e ação na tela de parceiros. Teste do domínio **2 passed**, `npm run typecheck` e `npm run build` verdes. Permanecem como próximas ondas a política administrativa completa de fidelidade, código de indicação diretamente no PDV, rentabilidade por obra/consumo e BI/E2E.
- **2026-09-02 (requisito de documentação final)**: definido como critério obrigatório de encerramento do plano o manual de uso para usuário final em formato wiki, em `docs/wiki/`, com índice, página por módulo/tela, explicações “o que é / para que serve / papel no sistema”, perfis e permissões, pré-requisitos, passo a passo, atalhos, exceções, auditoria e screenshots reais anonimizados de DEV. O manual será produzido após a estabilização dos módulos e validado contra a versão efetivamente entregue; `MANUAL.md` existente não é considerado substituto enquanto não cumprir essa cobertura.
- **2026-09-02 (início do manual wiki integrado)**: criada a wiki operacional em `docs/wiki/` com índice, capítulos por frente e instruções de captura/atualização; criada a central de ajuda pesquisável na rota pública `#/manual`, disponível pelo botão **Ajuda** no shell e pelo link da tela de login, com 28 entradas e atalho `Ctrl+/`. A rota pública não expõe dados de negócio e não é bloqueada pelo overlay de manutenção. Capturas reais da central foram salvas em `docs/wiki/capturas/manual-central-dev.png` e `manual-central-mobile-dev.png`; capturas autenticadas dos módulos permanecem pendentes porque o proxy DEV `/api` está retornando `502` no momento. Validação: `npm run typecheck` e `npm run build` verdes após correção de import `lazy`; nenhuma operação autenticada, deploy ou restart foi executada.
- **2026-09-02 (manual wiki — primeira entrega)**: ampliada a central para 30 referências de rota, incluindo Cotações e Solicitações legadas, e adicionados capítulos operacionais para todas as frentes do ERP: vendas, compras, cadastros, estoque, financeiro, preços, fiscal, relatórios, pós-venda e administração. A central funciona sem autenticação para consulta segura e não é coberta pelo overlay de manutenção; o acesso autenticado permanece disponível no topo. Capturas desktop/mobile da central foram validadas e versionadas. Validação final da entrega: **41 testes frontend passed**, `npm run typecheck`, `npm run build` e `git diff --check` verdes. Pendente para a segunda etapa: capturar cada módulo autenticado com massa DEV anonimizada e recuperar o proxy `/api` que apresentou `502` no acesso direto ao Vite; nenhum deploy ou restart foi executado.
- **2026-09-02 (manual wiki — capturas completas e auditoria visual)**: capturadas no DEV as 30 rotas principais, além de ações/subtelas de Pré-venda, Orçamentos, Pós-venda, Produtos, Clientes, Parceiros, Vendedores, Compras, Fornecedores, Estoque, Financeiro, Preços, Bancos e Fiscal. Dados de clientes, fornecedores, usuários, documentos e valores financeiros foram anonimizados no DOM antes da gravação; não houve alteração no banco. Criados `docs/wiki/capturas/README.md` como índice de cobertura e `docs/wiki/15-melhorias-ui-ux.md` com auditoria P0/P1/P2 baseada nas imagens, mantendo o padrão Salesforce Lightning/SLDS para tabelas e teclado. Achados relevantes: Relatórios exibiu “Sem acesso” para o administrador (pendência funcional de RBAC) e o editor de produto exige estado de carregamento observável; ambos foram registrados como defeitos, não mascarados por redesign. A wiki e `MANUAL.md` passaram a apontar para o índice visual. Nenhum deploy, staging ou produção foi acionado.
- **2026-09-02 (execução da auditoria visual P0/P1/P2)**: criada e aplicada a migração **0149** (`rbac_relatorios`), alinhado o gate `/api/relatorios` ao recurso `relatorios` e validado no DEV com schema **149**: administrador recebe `relatorios.visualizar` e os 6 endpoints da central retornam **200**. Frontend: navegação ganhou busca de módulo, grupos recolhíveis e breadcrumb; `Table` ganhou `scope=col` e foco visível; `Button`, abas e modal receberam foco consistente; `Loading`, `ErrorState`, retry e estados vazios foram padronizados; Painel e Relatórios passaram a exibir alertas acionáveis e erros recuperáveis. Relatórios recebeu tabs com teclado `←/→/Home/End`, KPIs e layout responsivo. Validação: **42 testes frontend**, `npm run typecheck`, `npm run build`, `py_compile` e links/imagens da wiki verdes. O container backend não possui `pytest`, então a suíte backend não rodou dentro dele; nenhuma publicação fora do DEV foi feita.
- **2026-09-02 (manual wiki HTML visual integrado)**: a central `#/manual` passou a exibir o manual em HTML com galerias incorporadas por módulo, incluindo telas principais, ações, consultas, abas, modais e estados contextuais. Foram empacotadas **66 capturas DEV anonimizadas** em `frontend/public/manual/capturas/`, com legenda, texto alternativo, ampliação e navegação de retorno; o manual não depende mais de links soltos para `docs/wiki`. Criados catálogo tipado `frontend/src/manual-capturas.ts` e regressões `frontend/tests/manual.test.ts`; capturas desktop/mobile da própria central foram atualizadas. Validação: **44 testes frontend**, `npm run typecheck`, `npm run build`, verificação de 64 referências sem arquivos ausentes e inspeção visual desktop/mobile. Nenhum deploy fora do DEV foi realizado.
- **2026-09-02 (auditoria e plano de relatórios analíticos/sintéticos)**: auditado o backend, frontend, contratos, modelo de clientes, impressão e referências de mercado. Confirmado que a implementação atual é um painel resumido: não há impressão/exportação de relatórios, paginação/detalhamento suficientes, ABC/XYZ, DRE analítica, filtros de clientes por tipo/segmento/aniversário ou extrato de compras por cliente; `data_nascimento` ainda não existe no modelo. Criados `RELATORIO_AUDITORIA_RELATORIOS.md` e `PLANO_RELATORIOS_ANALITICOS_SINTETICOS.md`, com benchmark TOTVS, backlog P0/P1/P2, arquitetura, orientação retrato/paisagem, APIs alvo, sprints, segurança/LGPD, critérios de aceite e testes. `docs/wiki/10-relatorios.md` foi alinhado ao estado real e passou a apontar para os novos documentos. Nenhum código funcional, migration, restart ou deploy foi executado nesta sessão.

- **2026-09-02 (execução do plano de relatórios — primeira entrega)**: criada e aplicada **somente no DEV** a migration `0150_clientes_relatorios` (schema DEV **150**, sem pendências), adicionando data de nascimento, consentimento de contato, canal preferencial, origem de cadastro e índices de relatório. Implementados serviços de clientes/aniversariantes e compras por cliente com filtros de tipo, segmento, categoria, localização, status, busca, aniversário, recência de compra, período e paginação; CPF/CNPJ saem mascarados. O cadastro de cliente passou a persistir e validar esses campos no backend. A central `#/relatorios` ganhou período, abas, KPIs, tabelas acessíveis, exportação CSV/XLSX e impressão/Salvar PDF pelo navegador; foram adicionadas páginas HTML A4 retrato/paisagem para dashboard, vendas, compras, estoque, financeiro e clientes. Exportações registram auditoria e têm proteção contra fórmula; rotas críticas aplicam RBAC `relatorios.exportar/financeiro`, e o contrato foi atualizado em `backend/openapi.json`. Validação: `44 testes frontend passed`, `npm run typecheck`, `npm run build`, `py_compile`, JSON OpenAPI, `git diff --check`; smoke autenticado no container DEV retornou **200** para central, relatórios, exportações e impressões. A suíte backend não foi executada porque a imagem DEV não possui `pytest`; nenhum deploy/staging/produção foi acionado. Pendentes: famílias analíticas completas de vendas/compras/estoque/financeiro, ABC/XYZ, necessidade de compra, DRE/rentabilidade detalhada, exportação assíncrona/PDF server-side, paginação visual e E2E backend.
- **2026-09-02 (correção de impressão/exportação dos relatórios)**: identificado que as páginas `/relatorios/...` não estavam no proxy nginx compartilhado; por isso `window.open` caía no fallback do Vite/SPA e exibia a tela de login mesmo com admin autenticado. Adicionada a rota backend no `frontend/nginx.backend-routes.conf`, validada com `nginx -t`, reiniciando somente o container frontend DEV. Validado também o fluxo real via `http://localhost:8080`: login **200**, impressão Dashboard **200** com HTML do Flask e exportação CSV **200** com Bearer; corrigido cabeçalho CSV duplicado. Páginas agora exigem `relatorios.imprimir` separado de `visualizar`, e o frontend reconhece as ações RBAC adicionais. Nenhum staging/produção foi alterado.
- **2026-09-02 (execução da onda P1 — vendas, compras e estoque analíticos)**: criados os contratos `vendas.analitico`, `compras.analitico` e `estoque.analitico`, com endpoints Flask, dispatcher, OpenAPI, exportação CSV/XLSX e impressão autenticada A4 paisagem. Vendas permitem agrupamento por produto/categoria/marca/cliente/segmento/vendedor/depósito/canal/condição e entregam quantidade, pedidos, clientes, receita, descontos, CMV e margem; compras mostram fornecedor, pedido/item, prazo, quantidade pedida/recebida/pendente e valores; estoque inclui cadastro ativo sem saldo, posição por depósito, ABC/XYZ, mínimo/máximo, disponibilidade, valor e situação. Frontend `#/relatorios` passou a consumir os contratos analíticos, preservando tabelas acessíveis e estados de erro/loading. Corrigido resumo de vendas para não duplicar pedidos/clientes quando um documento contém vários produtos. Validação: `44 testes frontend`, `npm run typecheck`, `npm run build`, `py_compile`, OpenAPI válido, migration DEV schema **150** sem pendências; smoke autenticado retornou **200** para APIs, exportações XLSX e impressões das três famílias. Suíte backend não executada por ausência de `pytest` no container. Pendentes reais: filtros avançados/paginação visual e drill-down na tela, ABC/XYZ histórica como relatório parametrizado, necessidade consolidada de compra, DRE/rentabilidade analítica, visões salvas, agendamento e E2E.
- **2026-09-02 (execução das pendências P1/P2.1 dos relatórios)**: vendas e compras ganharam pesquisa e paginação visual; estoque ganhou filtros por produto/ABC/situação e visão **Necessidade de compra**, que reutiliza o motor de reposição e expõe demanda, trânsito, lead time, ruptura provável e fornecedor sugerido. Compras passaram a exibir concentração por fornecedor e links para pedido impresso. Criado `financeiro.analitico` com aging não sobreposto na data de corte, títulos a receber/pagar abertos ou parciais, fluxo de caixa e DRE gerencial por competência operacional, com RBAC financeiro, exportação XLSX e impressão paisagem. Corrigido em smoke um `ORDER BY` incompatível com PostgreSQL antes de validar. Validação: **44 testes frontend**, `npm run typecheck`, `npm run build`, `py_compile`, OpenAPI válido e smoke autenticado 200 para necessidade, financeiro, exportação e impressão. Suíte backend não executada por ausência de `pytest` no container; nenhum deploy/staging/produção ou migration foi executado.
- **2026-09-02 (infraestrutura local de testes backend)**: o `backend/Dockerfile` passou a copiar `pyproject.toml`, manter `backend/tests/` na mesma estrutura do repositório e incluir `scripts/`; `pytest`/`pytest-cov` são instalados somente quando `INSTALL_TEST_DEPS=1`. O `docker-compose.yml` habilita esse argumento apenas no backend DEV, preservando a imagem de produção sem dependências de teste. A fixture `backend/tests/conftest.py` força `AUTO_MIGRATE=1` somente quando `TEST_PG_URL` aponta para o banco isolado, corrigindo o conflito com `AUTO_MIGRATE=0` usado pela aplicação. O `deployment/scripts/test-backend.sh` passou a construir imagem efêmera com dependências de teste, sem instalação ad-hoc nem montagem que quebrava `scripts/backup.py`. Backend DEV recriado; banco `catalog_test` criado. Validação: `pytest 9.1.1`, **555 testes coletados em 4,67s**, **12 testes direcionados passed**, `bash -n`, `docker compose config --quiet`, `py_compile` e `git diff --check` verdes. A suíte integral iniciou e revelou inicialmente a ausência de `scripts/backup.py` na imagem (`35 passed` antes da falha); após a correção, o teste de backup e os 12 direcionados passaram, mas a execução completa ainda não foi repetida até o fim. Nenhum staging/produção/deploy/migration não-dev foi acionado.
- **2026-09-02 (reforço visual UI/UX após inspeção autenticada)**: a auditoria visual no DEV confirmou que as melhorias anteriores estavam corretas, porém discretas. O shell ganhou sidebar corporativa azul-marinho com estado ativo mais evidente, busca e grupos com contraste reforçado, área de trabalho com fundo contextual e largura controlada, PageHeader/tabelas/cards com acabamento consistente e um painel operacional destacado no dashboard com próximos movimentos, alertas e atalhos para Relatórios/Compras. KPIs passaram a usar 3 colunas até telas 2XL e escala responsiva para não cortar valores monetários; em telas menores o hero reorganiza o resumo em duas colunas. As regras de teclado, foco, RBAC e contratos de API foram preservadas. Inspeção autenticada com usuário de teste mostrou a nova composição no Painel. Validação: **44 testes frontend passed**, `npm run typecheck`, `npm run build` e `git diff --check` verdes. Nenhum deploy, staging ou produção foi acionado.
- **2026-09-02 (SKU de acesso rápido)**: revisado o padrão automático para `GRUPO-SUBGRUPO-FAM[-VAR]`, com exemplos `ELE-CAB-001` e `ELE-CAB-001-02`. O cadastro normal deixou de usar o fallback longo `NOME-ID-ID`; o backend resolve grupo/subgrupo, agrupa produtos da mesma família, usa sufixo numérico para variações/colisões e serializa a escolha por família com advisory lock. Marca, bitola, cor, potência, rosca e fornecedor permanecem nos atributos/descrição pesquisáveis. SKUs manuais e já utilizados em operações continuam preservados. Preview/API e tela de produtos foram alinhados, regras foram registradas em `AGENT-produtos.md` e foram criadas regressões em `backend/tests/test_sku_acesso_rapido.py`. Validação: `py_compile`, `npm run typecheck`, `npm test -- --run` (**44 passed**), build backend com pytest e 2 testes puros de SKU passaram. O teste de integração que reconstrói o schema ficou bloqueado no scan da migração histórica 0096 de imagens; nenhum deploy, restart ou migration foi executado.
- **2026-09-02 (metodologia da planilha de precificação)**: estudada `docs/PLANILHA DE PRECIFICAÇÃO.xlsx` com seis abas. A metodologia foi incorporada ao ERP no motor autoritativo: custo líquido + embalagem/frete unitários, percentuais sobre a venda, despesa fixa real ou referência por atividade, divisor, preço mínimo, preço sugerido e cenário separado para IBS/CBS. Criadas as migrações `0151_metodologia_precificacao` e `0152_preserva_markup_legado`, aplicadas somente no DEV (schema **152**); tabelas antigas com markup sem margem preservam o comportamento legado. Criados endpoints `/api/precos/configuracao` e memória ampliada em `/api/precos/calcular/{produto_id}`, com validação de percentuais, alerta de divisor inválido e contexto fiscal. Frontend `#/precos` ganhou aba Premissas, simulador didático em etapas, fórmula visível, indicadores e leitura gerencial; tabelas exibem o método. Atualizados OpenAPI e wiki de preços. Validação: 3 testes puros da metodologia passaram no container, smoke autenticado da configuração/cálculo retornou 200, payload inválido retornou 400, `npm test -- --run` (**44 passed**), `npm run typecheck`, `npm run build`, `py_compile`, JSON OpenAPI e `git diff --check` verdes. A regressão de integração `test_precos.py` não concluiu dentro da janela por reconstrução/scan histórico da migration 0096; nenhuma publicação fora do DEV foi executada.
- **2026-09-02 (auditoria de completude da precificação)**: confirmado que a metodologia da planilha foi replicada parcialmente: custos fixos entram no divisor por referência de atividade ou relação despesa/faturamento; custos variáveis unitários entram quando informados, mas a despesa variável mensal ainda não é rateada automaticamente para evitar dupla contagem. A geração em lote continua usando uma margem comum e não possui margem-alvo individual persistida por produto/variação. Criado `docs/PLANO_PRECIFICACAO_COMPLETA_MARGEM_INDIVIDUAL.md` com regras de rateio, precedência de políticas, modelo de dados, APIs, fluxo atômico de lote, redesign, RBAC, auditoria, migrations Expand/Migrate/Contract, sprints e critérios de aceite. Nenhum código funcional, migration, restart ou deploy foi executado nesta etapa.
- **2026-09-03 (execução do plano de classificação financeira)**: aplicadas somente no DEV as migrations **0153** (`classificacao_despesas`) e **0154** (`config_precificacao_competencia`), levando o schema DEV à versão **154**. O Plano de Contas agora exige natureza/política para despesas, com filtros de natureza/rateabilidade e consulta de uso histórico; Contas a Pagar grava competência, centro de custo, snapshots, elegibilidade, origem e status de classificação. Lançamento manual exige conta de despesa e validação backend; recebimentos de compras herdam regra ativa do fornecedor com vigência, sem alterar histórico; lotes de contas a receber foram mantidos compatíveis após correção de regressão. Criada central financeira para pendências, classificação, rateio 100%, competências, apuração e fechamento/reabertura; regras de fornecedor também configuráveis no cadastro. Pagamento de título novo classificado como pendente/rejeitado é bloqueado; aprovação de classificação e competência usa RBAC financeiro, e alterações são auditadas em `auditoria_evento`. A precificação passa a consumir somente competência aprovada/fechada quando habilitada, incluindo variáveis rateadas sob opção explícita, com origem e alertas de fallback/ausência de competência; o endpoint legado de geração mantém fórmula histórica para não alterar preços silenciosamente, enquanto prévia/reajuste auditado usa o motor completo. OpenAPI atualizado para **147 paths**, incluindo plano, classificação, rateio, competências, memória e regras de fornecedor. Validação: `py_compile`, JSON OpenAPI, `git diff --check`, frontend `npm run typecheck`, `npm test -- --run` (**44 passed**), `npm run build`, smoke autenticado DEV (`200` nas consultas novas), testes backend direcionados (**9 passed**), testes de preços/metodologia/classificação (**13 passed**) e fluxos de recebimento/lote/devolução/três vias (**28 passed**). Nenhum staging/produção/deploy foi acionado.
